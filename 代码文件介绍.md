# GAPS 项目代码、实验与交接手册

> 最后核对日期：2026-08-12（Asia/Shanghai）
>
> 当前工作树：`D:\A Python learning\Federated Learning\TRAE SOLO\.worktrees\iotj-final-classification-le1`
>
> 当前分支：`codex/iotj-final-classification-le1`
>
> GitHub：`https://github.com/Hjh-fy/GAPS.git`
>
> 当前科学结果基线：`c6055ea`（R0 exact-recovery fail-closed）
>
> R0-v2 已在授权 `1fc48c1`（完整 SHA `1fc48c1003c96e235ae123481669b650ba8f90d6`）完成 formal source-only execution；结果根已封存为不可变证据。其后仅允许只读结果分析、文档和证据一致性维护，不得重跑、覆盖或改写结果。

本文是新对话、新成员和自动化任务接手 GAPS 项目的**第一入口**。它说明当前事实、冻结协议、代码入口、三机拓扑、运行命令、结果位置、已知局限和停止条件。

若本文与 2026 年 7 月的 `README.md`、旧 handoff、legacy 数据说明或历史结果冲突，以以下来源为准：

1. 当前研究的 `protocol_manifest.json`；
2. 当前研究的 `EXPERIMENT_REGISTRY.csv`；
3. 当前结果目录中的严格审计与 SHA256 索引；
4. 本文的“当前进展”与“算力和三机拓扑”章节；
5. Git 提交历史。

旧 `README.md` 中 B5、H8、Runtime v5 等内容仍可用于理解历史代码，但不是 canonical-v1 当前科学状态。

---

## 1. 新对话 60 秒快速接手

### 1.1 先进入正确工作树

```powershell
Set-Location 'D:\A Python learning\Federated Learning\TRAE SOLO\.worktrees\iotj-final-classification-le1'
git status --short --branch
git log -8 --oneline
```

预期分支为：

```text
codex/iotj-final-classification-le1
```

不要在仓库主目录、其他 worktree 或旧分支直接启动正式实验。

### 1.2 读取当前唯一状态源

```powershell
Get-Content -Raw 'docs/experiments/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812/protocol_manifest.json'
Get-Content -Raw 'docs/experiments/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812/PROTOCOL.md'
Get-Content -Raw 'docs/experiments/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/protocol_manifest.json'
Get-Content -Raw 'docs/experiments/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/EXPERIMENT_REGISTRY.csv'
Get-Content -Raw 'results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/R0_EXPERIMENT_AUDIT.md'
Get-Content -Raw 'results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/R0_NUMERICAL_FAILURE_ANALYSIS.md'
```

### 1.3 当前结论一句话版

```text
C0 已完成并保留 interleaved A4；原 R0 canonical FedRidge exact-recovery 严格门失败且证据原样保留。
独立版本 R0-v2 已 formal PASS：`FEDRIDGE_ALGEBRAIC_EXACT_NUMERICAL_EQUIVALENCE_ESTABLISHED`。
结果路径已存在且不可变；R0-v2 为 `PASS`，R1 已完成且结论为 `DEVICE_DEPENDENT`，R2 已触发但尚无结果；Q0、Q1 仍未开放。当前没有正在运行的正式训练。
```

### 1.4 先验证 canonical 数据，不要先训练

```powershell
python tools/verify_iotj_canonical_v1_hashes.py dataset/iotj_canonical_v1
```

canonical-v1 聚合 SHA256 必须是：

```text
2f810d7e93cae5f361923184e9dc87d5ae59e0f59be9f52aff7e14f9f33e94f6
```

### 1.5 当前默认动作

- 可以：只读检查、结果分析、文档和证据一致性审计。
- 可以：运行本文列出的本地 pytest、compileall、canonical hash 和静态代码审计；这些不是 formal R0-v2 execution。
- 不可以：再次运行 R0-v2 `run`/正式 `preflight`/`audit`、自动训练、修改协议、放宽 R0/R0-v2 阈值、重跑已完成端点、打开 R1/Q0、继续新算法搜索。
- R0-v2 已在授权 `1fc48c1` 完成；此前授权只覆盖该已封存端点，不能解释为 R1 或其他下游执行授权。

---

## 2. 项目目标和当前主线

GAPS 面向跨设备电子鼻/气体传感的云—边—传感器生命周期，主要研究：

```text
canonical 数据预处理
→ C1/C2 source Flower 联邦分类
→ C3/C4/C5 target commissioning
→ 气体类别路由
→ 定量浓度回归
→ QC accept/review/reject
→ 通信、Pi 5 部署和投稿证据
```

当前论文不能笼统声称“GAPS 在所有方面优于全部基线”。目前证据更支持：

- lifecycle / commissioning / observability 框架；
- frozen endpoint 下的高分类性能；
- 路由错误会显著影响下游定量回归；
- 部分旧回归结果具有描述价值，但 canonical quantitative evidence 仍需完成严格重建；
- 新算法探索已按注册停止规则关闭。

---

## 3. canonical-v1 冻结协议

### 3.1 数据与角色

| 项目 | 冻结值 |
|---|---|
| 数据集 | `dataset/iotj_canonical_v1` |
| 预处理 | `HZ5_MEAN_W10S` |
| 采样率 | 5 Hz |
| 窗口 | 10 s |
| 步长 | 5 s |
| 张量形状 | `50 × 8` |
| source | C1、C2 |
| target | C3、C4、C5 |
| target split | 约 20% calibration、80% sealed test |
| seed | 42（除已注册 multi-seed 研究外） |

三个 target 的 calibration/test 身份由 manifest 冻结。不得重新随机划分、交换角色或根据 test 结果调整 calibration。

### 3.2 正式 A4 分类配置

| 项目 | 冻结值 |
|---|---|
| 方法 | A4 GAPS router |
| source rounds | 25 |
| local epochs | 1 |
| batch size | 32 |
| optimizer | Adam |
| learning rate | `5e-4` |
| aggregation | 全程普通 FedAvg；selective aggregation 关闭 |
| target adaptation | 每轮固定 100 steps（interleaved） |
| checkpoint | 固定 round 25 |
| 调参 | 禁止使用 target test；不做事后搜索 |

目标 adaptation 可使用该方法注册允许的 target calibration `x/class/phase`，不使用 target concentration。Target test 只能在所有固定端点锁定后一次性评估。

### 3.3 quantitative feature 边界

- 83D：传感器统计特征。
- 104D H1：83D 传感器特征 + 21D metadata/phase 特征。
- 21D 主要是 protocol/phase/metadata 描述，不等同于额外 21 个传感器通道。
- slope、`absdiff_mean/max` 等只解释为 **fixed-5-Hz discrete dynamic descriptors**。
- 不声明这些特征 sampling-rate invariant。
- 不声明旧 10 Hz/100×8 与 canonical 5 Hz/50×8 数值等价。

本阶段禁止复用任何由 legacy 10 Hz/100×8 产生的 H1/83D cache、scaler、sufficient statistics、Ridge alpha、FedRidge/R84 coefficient、QC normalization 或 QC threshold。

---

## 4. 当前实验进展

### 4.1 当前研究：Canonical Regression Reconstruction + QC Necessity

结果根目录：

```text
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/
```

协议根目录：

```text
docs/experiments/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/
```

当前 manifest 状态：`R0_FAIL_CLOSED_EXACT_RECOVERY_R1_BLOCKED`。

| 阶段 | 状态 | 结论/下一门槛 |
|---|---|---|
| C0 adaptation timing | 完成 | `V1_INTERLEAVED_RETAINED` |
| R0 canonical reconstruction | `FAIL_CLOSED` | exact-recovery 严格门失败 |
| R1 83D vs R84 | 完成、审计 PASS | `CANONICAL_R84_DEVICE_DEPENDENT` |
| R2 negative-transfer 分支 | 已触发、尚无结果 | 等待独立执行与审计 |
| Q0 QC necessity | 未开始、被阻塞 | regression freeze 后允许 |
| Q1 uncertainty QC | 未开始 | 仅 Q0 注册条件触发 |

### 4.2 C0 结果

C0 的唯一变量是 target adaptation timing：

- A：正式 interleaved A4；每轮进行 target adaptation。
- B：同一 source 训练配置和完整 source context，round 25 后仅进行一次 100-step final adaptation。

final-only 相对 interleaved 的 Macro-F1 变化：

| Target | Δ Macro-F1（final-only − interleaved） | 门槛 | 结论 |
|---|---:|---:|---|
| C3 | -0.009738 | -0.005 | FAIL |
| C4 | -0.009589 | -0.005 | FAIL |
| C5 | -0.053484 | -0.005 | FAIL |

因此保留 interleaved A4，不进行 rescue search。

关键证据：

```text
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/C0/C0_DECISION.json
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/C0/C0_EXPERIMENT_AUDIT.md
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/C0/classification_v1_timing_comparison.csv
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/C0/C0_SHA256_INDEX.json
```

### 4.3 R0 结果和当前阻塞点

R0 从 canonical 50×8 窗口重新构建 source quantitative features，并检查 pooled Ridge 与 sufficient-statistics FedRidge 的 exact recovery。

注册得到的 source alpha 一致：

| Gas | alpha |
|---|---:|
| Ethanol | 0.1 |
| CO | 0.01 |
| Ethylene | 0.01 |
| Methane | 0.01 |

严格门失败项：

- CO coefficient 最大差 `6.100162863731384e-08`，门槛 `1e-08`；
- Ethylene coefficient 最大差 `2.0023435354232788e-08`，门槛 `1e-08`；
- Methane scaler 最大差 `0.999999831412606`，门槛 `1e-10`；
- Methane coefficient 最大差 `0.0018461935413379388`，门槛 `1e-08`。

prediction 最大差仅 `2.5330280095658964e-08 ppm`，小于 `1e-06 ppm`，但协议明确禁止以 practical equivalence 替代 exact-recovery 门。

已定位的主要数值机制是近常量 `window_len_s`：pooled 直接标准差会落到 scale floor 的 1.0，而 `sum_x2/n - mean²` 存在消减误差，得到约 `1.6859e-07` 的伪尺度。

R0 已严格停止：

- 未打开 source test labels；
- 未创建 C3/C4/C5 target feature cache；
- 未打开 target test labels；
- 未运行 R1；
- 未放宽阈值；
- 未使用 practical-equivalence fallback；
- 未重跑。

关键证据：

```text
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/R0_FAILURE_AUDIT.json
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/R0_NUMERICAL_FAILURE_ANALYSIS.md
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/R0_EXPERIMENT_AUDIT.md
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/canonical_fedridge_exact_recovery.json
results/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/R0/R0_SHA256_INDEX.json
```

### 4.4 独立 R0-v2：formal PASS，结果根已封存

R0-v2 是对原 R0 数值重建问题的**独立版本化协议**，已在授权
`1fc48c1` 下完成 formal source-only execution：

```text
FEDRIDGE_ALGEBRAIC_EXACT_NUMERICAL_EQUIVALENCE_ESTABLISHED
R0_V2_EXPERIMENT_AUDIT=PASS
R1_released=true; downstream_launched=false
```

它不修改、覆盖或重新解释原 R0；原 R0 仍是 `R0_EXACT_RECOVERY_NOT_ESTABLISHED`，其代码、结果、阈值、审计和 SHA256 index 均保留。R0-v2 为 `PASS`；R1 已完成且为 `DEVICE_DEPENDENT`；R2 已触发但尚无结果，Q0、Q1 仍未开放。

R0-v2 的执行边界是严格 source-only：

- 有序 source 客户端仅为 `C1,C2`，聚合顺序固定为 C1→C2；
- 不加载、枚举或散列 C3/C4/C5 target 文件字节，不创建 target cache，不读取 target label；
- 不运行 QC，不使用 Q0/Q1，不新增 target/QC 参数或路径；
- C1/C2 的完整 expected-hash map 固定为 32 项：preflight 锁前只读取并验证 22 项 train/calibration 与 stats 文件；其余 10 项 source-test 文件只登记 expected hash，不在锁前读取；
- C1/C2 train/calibration cache 将来必须在新结果根内 fresh build，不能复用原 R0 cache；source test 只能在 alpha/model lock 后打开；首次 source-test cache/data request 前必须原子发布 `source_test_validation_receipt.json`，记录两份 lock SHA 和 10 项有序锁后验证结果。

新增代码入口：

| 文件 | 功能 |
|---|---|
| `gaps_flower/canonical_fedridge_v2.py` | float64 mergeable central moments、稳定 scaler、FedRidge/pooled reconstruction、诊断和两值决策门 |
| `gaps_flower/canonical_quantitative_features.py` | 保留旧默认行为，并支持显式 R0-v2 study ID 的 cache provenance 验证 |
| `scripts/run_iotj_canonical_fedridge_r0_v2.py` | fail-closed `preflight/run/audit` controller；正式 `preflight/run` 必须带授权 freeze commit |
| `tests/test_iotj_canonical_fedridge_r0_v2.py` | source-only、数值稳定、协议、runner、evidence/audit 安全回归测试 |

协议 bundle 位于 `docs/experiments/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812/`，严格包含以下 8 个版本化文件：

1. `protocol_manifest.json`
2. `PROTOCOL.md`
3. `EXPERIMENT_PLAN.md`
4. `EXPERIMENT_MATRIX.csv`
5. `EXPERIMENT_REGISTRY.csv`
6. `NEAR_CONSTANT_SCALE_POLICY.md`
7. `R0_V2_NUMERICAL_TOLERANCE_JUSTIFICATION.md`
8. `FEDRIDGE_NUMERICAL_STABILITY_MANUSCRIPT_NOTE.md`

冻结数值与 near-constant 门禁：

- 全部数值计算为 float64；local/merge 使用 central moments，population variance 为 `max(M2/n, 0)`；禁止 `sum_x2/n - mean²` raw-moment 方差；
- `raw_scale < 1e-9` 时 canonical scale 固定替换为 `1.0`，等于 `1e-9` 不替换；`1e-9` 继承既有协议，未根据本次数据或性能选择/调优；
- alpha grid 固定为 `(0, 0.01, 0.1, 1, 10, 100, 1000)`，source train/source calibration 选择、first-in-grid tie break、`numpy.linalg.pinv`、intercept 不正则化；
- `tau_moment=64*gamma(1340)`，`tau_residual=128*gamma(105)`，raw/clipped prediction 与 RMSE/MAE functional parity 门为 `1e-6 ppm`，condition 门为 finite `kappa` 且 `kappa*epsilon < 1`；
- alpha/scaler/safe-scale mask/normal equations/condition/two residuals/raw+clipped prediction/RMSE/MAE/finite 均为独立 hard gates；coefficient difference 只作诊断，不是 hard gate。

正式结果根已建立、被 Git 忽略并作为不可变 evidence root 保留：

```text
results/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812/
```

已完成端点不得重跑；其历史命令形状仅供理解。结果根包含 34 个文件、7,453,988 bytes，`sha256_index.json` 索引 32 个 evidence 文件，index/completion marker 为保留的两个未索引文件。控制器 audit 恰执行一次，PASS 后前/后 34-file hashes 一致。

### 4.5 其他已完成研究及科学使用边界

| 研究 | 结果位置 | 当前用途 |
|---|---|---|
| canonical-v1 分类、部署与投稿证据 | `results/iotj_canonical_v1_final_20260808/` | 正式 canonical 分类证据 |
| C5 5/10/15/20% label budget | `results/iotj_canonical_v1_c5_budget_20260810/` | `LABEL_EFFICIENCY_NOT_SUPPORTED` |
| 方法突破/多阶段验证 | `results/iotj_canonical_v1_method_breakthrough_20260811/` | Story E，`STOP_NEW_ALGORITHMS` |
| 方法生命周期重构 G1/G2/G3 | `results/iotj_canonical_v1_method_redesign_20260811/` | 历史方法验证证据 |
| A0T vs A4 regression | `results/iotj_canonical_v1_final/a0t_vs_a4_regression/` | 描述性 routing→regression 证据 |
| 旧 regression/QC closure | `results/iotj_canonical_v1_final/regression_qc_closure_20260811/` | provenance hard-fail/无效化证据 |

重要：`a0t_vs_a4_regression` 曾得到：

- C5 S_ALL RMSE：A0T `22.1560 ppm`，A4 `18.4765 ppm`，差 `-3.6794 ppm`；
- pooled C3/C4/C5：A0T `15.1925 ppm`，A4 `13.3144 ppm`，差 `-1.8781 ppm`；
- 正确路由后的 mapping 并未改善，收益主要来自 routing error 的身份/严重程度变化；
- C5 CO 与 Methane 是不利子组。

但是，后续 provenance audit 发现旧 quantitative assets 混入 legacy 100×8 H1。因而这些数值只能作为**描述性诊断和重建目标**，不能绕过当前 R0，也不能自动作为最终 canonical regression/QC 论文证据。

### 4.6 分类侧已冻结的关键结论

- interleaved A4 C3/C4/C5 Macro-F1 分别约为 `0.998507 / 0.997794 / 0.994126`。
- C5 label budget 20/15/10/5% 下，A4 Macro-F1 为 `99.4126% / 98.6778% / 97.8016% / 96.7031%`。
- 相同预算下 A4 均未优于 A0T，最终结论为 `LABEL_EFFICIENCY_NOT_SUPPORTED`。
- source-DG 多 seed 未确认，commissioning bridge 未支持，cost-aware routing 未支持。
- 当前终止动作是 `STOP_NEW_ALGORITHMS`。

---

## 5. 代码目录和主要文件

### 5.1 顶层目录

```text
gaps_flower/   Flower 客户端、服务器、聚合、DA、SCAFFOLD、回归与特征逻辑
gaps_deploy/   部署推理、QC、运行时和安全边界
scripts/       正式实验控制器、评估、分析、审计、打包和作图入口
tools/         数据/hash 等独立验证工具
tests/         协议、泄漏、数学、运行时、结果和审计测试
docs/          冻结协议、manifest、实验注册表和设计文档
dataset/       本地数据资产；正式运行前必须核验 hash
results/       实验输出、checkpoint、CSV、报告、日志和 SHA256 索引
release/       已冻结部署包；注意部分属于 legacy 主线
```

### 5.2 分类与 Flower 核心

| 文件 | 功能 |
|---|---|
| `gaps_flower/client_app.py` | source client 训练入口；local epochs、batch、optimizer、FedProx/SCAFFOLD 等 |
| `gaps_flower/server_app.py` | Flower server 入口；轮数、聚合、server adaptation、checkpoint |
| `gaps_flower/strategy.py` | FedAvg/selective aggregation 与 server state 管理 |
| `gaps_flower/task.py` | 模型、dataset、loss 和 client task 构建 |
| `gaps_flower/domain_adaptation.py` | GAPS 内部 server DA 与 label-conditioned loss |
| `gaps_flower/canonical_uda.py` | canonical CORAL/MMD/DANN x-only baselines |
| `gaps_flower/domain_adaptation_inputs.py` | target 信息访问契约和数组加载 |
| `gaps_flower/loss_activity.py` | loss configured/available/active 审计 |
| `gaps_flower/target_information.py` | method-specific target field gate |
| `gaps_flower/scaffold.py` | canonical SGD-style SCAFFOLD control variates |
| `gaps_flower/state_fingerprint.py` | ordered state-content fingerprint |
| `gaps_flower/evaluate_checkpoint.py` | checkpoint 加载和分类评估 |

### 5.3 当前 regression reconstruction 核心

| 文件 | 功能 |
|---|---|
| `gaps_flower/canonical_quantitative_features.py` | 从 canonical 50×8 窗口重算 83D/104D quantitative features |
| `gaps_flower/canonical_fedridge.py` | 原 R0 source-only pooled/FedRidge；必须保留，不由 R0-v2 覆盖 |
| `gaps_flower/canonical_fedridge_v2.py` | R0-v2 stable central moments、Ridge reconstruction、数值诊断与 gate |
| `run_regression_head_ablation.py` | `rich_feature_dict` 与历史 feature-name 定义来源 |
| `scripts/run_iotj_canonical_regression_reconstruction_c0.py` | C0 timing gate 控制器 |
| `scripts/run_iotj_canonical_regression_reconstruction_r0.py` | R0 feature/FedRidge exact-recovery gate |
| `scripts/run_iotj_canonical_fedridge_r0_v2.py` | R0-v2 fail-closed preflight/run/audit；当前禁止正式调用 |
| `tests/test_iotj_canonical_regression_reconstruction.py` | C0/R0 TDD、泄漏、feature、门槛和审计测试 |
| `tests/test_iotj_canonical_fedridge_r0_v2.py` | R0-v2 数值、source-only、protocol、runner 与 evidence audit 测试 |

### 5.4 已冻结 canonical 分类入口

| 文件 | 功能 |
|---|---|
| `scripts/run_iotj_canonical_v1_classification.py` | C3/C4/C5 A4 正式分布式分类 |
| `scripts/evaluate_iotj_canonical_v1_classification.py` | 固定 endpoint 分类评估 |
| `scripts/run_iotj_canonical_v1_comparators.py` | FedAvg/FedProx/SCAFFOLD 等基线 |
| `scripts/run_iotj_canonical_v1_a0t.py` | equal-label A0T |
| `scripts/run_iotj_c5_label_budget.py` | C5 5/10/15% 六个完整 25-round 端点 |
| `scripts/evaluate_iotj_c5_label_budget.py` | label-budget 一次性评估 |
| `scripts/analyze_iotj_c5_label_budget.py` | label-budget 汇总和结论 |

这些正式输出已经存在，不得直接再次运行；只有新 study ID、新冻结 manifest 和明确授权才能建立新端点。

### 5.5 部署和投稿证据入口

```text
scripts/build_iotj_canonical_v1_deployment.py
scripts/benchmark_iotj_canonical_v1_pi5.py
scripts/build_iotj_canonical_v1_submission_bundle.py
scripts/finalize_iotj_canonical_v1_evidence.py
scripts/finalize_submission_evidence_closure.py
```

执行前必须确认输入 checkpoint、feature、QC 是否属于当前 canonical 证据链，不能用脚本名称中的 `final` 推断资产仍然有效。

---

## 6. 整体实验工作流

正式实验应按以下状态机推进：

```text
科学问题
→ experiment ID / hypothesis / decision gate
→ protocol manifest + registry + stop rule
→ TDD 与静态 leakage audit
→ 数据和远程环境 preflight
→ pre-run freeze commit
→ 创建不可变 formal lock
→ 三机固定端点运行
→ completion marker / checkpoint fingerprint
→ 所有端点齐全后一次性打开 sealed test
→ evaluate → analyze → strict audit
→ pytest + compileall + SHA256 verification
→ 只提交预期的紧凑证据
→ push GitHub
→ 达到 stop rule 后停止
```

### 6.1 设计阶段必须冻结

- 数据集 aggregate hash 和每个 split manifest hash；
- source/target role；
- target calibration/test 身份；
- seed、rounds、local epochs、batch size；
- optimizer、LR、adaptation steps 和 loss coefficients；
- checkpoint endpoint 和 selection rule；
- target field access（x/class/phase/concentration/test）；
- 主要指标、异常指标和决策阈值；
- 条件分支和停止条件；
- output root、completion marker 和 audit 文件名。

### 6.2 preflight

至少检查：

- 当前分支、HEAD 和工作树；
- canonical 数据 hash；
- 三个节点可达；
- 三个专用 Python 环境可导入依赖；
- 远端 canonical 数据根存在且一致；
- 没有残留 Flower process；
- 本地和远端正式输出目录不存在；
- command 不包含 resume/旧 checkpoint；
- protocol fingerprint 与 freeze commit 一致。

### 6.3 运行和监控

常见证据：

```text
RUN_PROGRESS.json
runner.pid / source_runner.pid
runner.stdout.log / runner.stderr.log
server.stdout.log / server.stderr.log
client_c1.stdout.log / client_c1.stderr.log
client_c2.stdout.log / client_c2.stderr.log
fixed_endpoint_complete.json
SEALED_TEST_OPEN.json
*_SHA256_INDEX.json
STRICT_AUDIT.json 或 *_EXPERIMENT_AUDIT.md
```

PID 只能说明曾启动过，不能单独证明仍在运行。必须同时检查：

- 本地进程是否存在；
- 日志是否继续增长；
- remote Flower round 是否推进；
- completion marker 是否存在；
- stderr 是否出现异常。

### 6.4 失败处理

- 保存 stderr、stdout、partial output、PID、远程日志和 hash；
- 先做 provenance/pipeline/systematic audit；
- 已有 completion marker 的端点绝不重跑；
- 不通过调参、改数据、换 checkpoint 或放宽门槛“修复”正式结果；
- 若协议本身需要修改，先形成 amendment 和新的 pre-run freeze commit；
- 失败门要求停止时必须 fail closed。

---

## 7. 三机拓扑、远程连接和算力说明

### 7.1 正式拓扑

| 角色 | SSH | 仓库/运行根 | 专用 Python | 算力用途 |
|---|---|---|---|---|
| Flower server + server DA | `root@121.40.139.213` | `/root/GAPS` | `/root/gaps_env/bin/python` | CPU server/DA |
| C1 source client | `gaps@192.168.137.172` | `/home/gaps/GAPS` | `/home/gaps/GAPS/gaps_rpi_env/bin/python` | Raspberry Pi CPU |
| C2 source client | `root@114.55.171.63` | `/root/GAPS` | `/root/gaps_c2_cpu_env/bin/python` | ECS CPU client |
| Windows 控制端 | 当前 worktree | 本地 worktree | 当前项目 Python | 编排、审计、评估、证据 |

正式 Flower 客户端命令明确使用 `--device cpu`。这个实验体系不依赖 CUDA 才能运行。

### 7.2 必须避免的算力误判

以下推断是错误的：

```text
Windows 没有 CUDA
+ 121.40.139.213 没有 nvidia-smi
+ 121.40 的系统 python 没有 torch
⇒ GAPS 三机训练无法运行
```

原因：

1. `121.40.139.213` 是 Flower server/server DA 节点，不是唯一训练节点；
2. source client 是 Pi C1 和 ECS C2；
3. 三个节点按协议使用各自专用虚拟环境，不使用系统 `python`；
4. 当前冻结协议本来就是 CPU 分布式拓扑；
5. Windows 主要负责编排、同步、监控、评估和审计。

所以遇到“本机无 CUDA/系统 Python 无 PyTorch”时，不要立即询问新的 CUDA 节点。应先核对下面三台主机和专用 Python。

### 7.3 SSH 连通性检查

```powershell
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@121.40.139.213 "date -u; hostname"
ssh -n -o BatchMode=yes -o ConnectTimeout=20 gaps@192.168.137.172 "date -u; hostname"
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@114.55.171.63 "date -u; hostname"
```

本文不保存密码、私钥、token 或其他凭据。SSH 应使用已配置的 key/agent。

### 7.4 专用环境检查

```powershell
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@121.40.139.213 "/root/gaps_env/bin/python -c 'import sys,torch,flwr; print(sys.executable); print(torch.__version__); print(flwr.__version__)'"
ssh -n -o BatchMode=yes -o ConnectTimeout=20 gaps@192.168.137.172 "/home/gaps/GAPS/gaps_rpi_env/bin/python -c 'import sys,torch,flwr; print(sys.executable); print(torch.__version__); print(flwr.__version__)'"
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@114.55.171.63 "/root/gaps_c2_cpu_env/bin/python -c 'import sys,torch,flwr; print(sys.executable); print(torch.__version__); print(flwr.__version__)'"
```

不要用下面这种检查替代正式环境检查：

```text
python -c "import torch"
nvidia-smi
```

2026-08-12 的只读实测结果：

| 节点 | 专用环境 PyTorch | Flower | canonical 数据 | 残留 Flower 进程 |
|---|---|---|---|---|
| `121.40.139.213` | `2.12.0+cu130` | `1.23.0` | PRESENT | 无 |
| Pi C1 | `2.12.1+cpu` | `1.23.0` | PRESENT | 无 |
| ECS C2 | `2.12.0+cpu` | `1.23.0` | PRESENT | 无 |

`121.40.139.213` 的 venv 使用 CUDA build 的 PyTorch，并不表示当前协议需要或实际启用了 GPU；它直接证明“系统 Python 没有 PyTorch”不能代表专用实验环境不可用。

### 7.5 canonical 数据位置

```text
Windows: dataset/iotj_canonical_v1
Pi C1:  /home/gaps/GAPS/flower_runtime/dataset/iotj_canonical_v1
ECS C2: /root/GAPS/confirmation_c2_data/iotj_canonical_v1
Server: /root/GAPS/dataset/iotj_canonical_v1（需要 server-side 数据时）
```

只检查目录存在还不够；正式 preflight 必须核验 manifest 和 aggregate hash。

### 7.6 残留进程检查

```powershell
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@121.40.139.213 "pgrep -af 'gaps_flower.(server_app|client_app)' || true"
ssh -n -o BatchMode=yes -o ConnectTimeout=20 gaps@192.168.137.172 "pgrep -af 'gaps_flower.(server_app|client_app)' || true"
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@114.55.171.63 "pgrep -af 'gaps_flower.(server_app|client_app)' || true"
```

发现残留进程时先确认所有权、run ID、PID/PGID 和日志，不能使用宽泛 `pkill python`。

### 7.7 Flower 连接方式

- server 在 `121.40.139.213` 上监听 `0.0.0.0:8080`；
- Windows 控制器建立 loopback-only SSH tunnels；
- Pi C1 和 ECS C2 都通过各自的 `127.0.0.1:18080` 连接 server；
- 正式控制器会部署 freeze commit 的 source archive 到 hash 命名 runtime，而不是依赖远端 `/root/GAPS` 当前 checkout。

隧道逻辑在：

```text
scripts/run_iotj_confirmation_observability.py::build_ecs_c2_tunnel_commands
```

通常不要手工建立隧道，正式 runner 会负责启动和清理。

### 7.8 Pi IP 注意事项

当前冻结/验证地址是 `192.168.137.172`。它属于局域网/热点地址，网络变化时可能不可达。不可达时先确认 Pi 网络和实际地址；不要未经审计直接修改正式 manifest 或已冻结 command。

---

## 8. 常用命令

以下命令按“只读/验证”和“正式运行入口”分开。当前状态下只建议执行前一类。

### 8.1 安全的本地只读检查

```powershell
Set-Location 'D:\A Python learning\Federated Learning\TRAE SOLO\.worktrees\iotj-final-classification-le1'
git status --short --branch
git log -8 --oneline
python tools/verify_iotj_canonical_v1_hashes.py dataset/iotj_canonical_v1
Get-Content -Raw 'docs/experiments/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/protocol_manifest.json'
Get-Content -Raw 'docs/experiments/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/EXPERIMENT_REGISTRY.csv'
```

### 8.2 当前相关测试

```powershell
python -m pytest -q tests/test_iotj_canonical_regression_reconstruction.py
python -m compileall -q gaps_flower scripts tools tests
```

大范围回归测试可按任务选择相关测试文件，不应因为临时 pytest 目录很多就直接删除用户资产。

### 8.3 查看本地进度文件

```powershell
Get-Content -Raw '<result-root>/RUN_PROGRESS.json'
Get-Content -Tail 100 '<result-root>/runner.stdout.log'
Get-Content -Tail 100 '<result-root>/runner.stderr.log'
Get-Content -Raw '<result-root>/runner.pid'
Get-ChildItem -Recurse '<result-root>' -Filter 'fixed_endpoint_complete.json'
```

若没有 `RUN_PROGRESS.json`，查看 study-specific 的 `source_runner.*`、server/client logs、completion markers 和 audit。

### 8.4 查看远程 round 进度

```powershell
ssh -n -o BatchMode=yes -o ConnectTimeout=20 root@121.40.139.213 "pgrep -af 'gaps_flower.server_app' || true; find /root/GAPS -name 'server_round_*.pth' -type f 2>/dev/null | tail"
```

正式 run 的精确 remote output 应从 `locked_run_spec.json` 或 runner 源码读取，不要猜路径。

### 8.5 C0 控制器命令（仅供复现理解，当前禁止重跑）

```powershell
python scripts/run_iotj_canonical_regression_reconstruction_c0.py preflight --device cpu
python scripts/run_iotj_canonical_regression_reconstruction_c0.py run-source --device cpu
python scripts/run_iotj_canonical_regression_reconstruction_c0.py adapt-targets --device cpu
python scripts/run_iotj_canonical_regression_reconstruction_c0.py evaluate --device cpu
python scripts/run_iotj_canonical_regression_reconstruction_c0.py audit --device cpu
```

默认主机：

```text
--ecs-host root@121.40.139.213
--pi-host gaps@192.168.137.172
--c2-host root@114.55.171.63
```

C0 已完成并有不可变证据。再次运行会破坏“不可重复端点”边界。

### 8.6 R0 控制器命令（仅供复现理解，当前禁止重跑）

```powershell
python scripts/run_iotj_canonical_regression_reconstruction_r0.py run --data-root dataset/iotj_canonical_v1
python scripts/run_iotj_canonical_regression_reconstruction_r0.py audit --data-root dataset/iotj_canonical_v1
```

R0 output 已非空且 protocol 状态已经是 fail-closed；runner 本身也会拒绝覆盖。若未来修改 exact-recovery 数值协议，必须创建新的 study/output root，而不是删除当前 R0 结果重跑。

### 8.7 R0-v2 已完成端点命令形状（禁止重跑）

runner 的实际 argparse 只暴露位置参数 `{preflight,run,audit}` 以及 `--data-root`、`--output`、`--authorized-freeze-commit`；`preflight` 和 `run` 均强制要求授权 SHA。可安全查看：

```powershell
python scripts/run_iotj_canonical_fedridge_r0_v2.py --help
```

以下是**已完成端点且仅供交接说明**的正式 `run` 命令形状，不是当前运行指令：

```powershell
python scripts/run_iotj_canonical_fedridge_r0_v2.py run --data-root dataset/iotj_canonical_v1 --output results/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812 --authorized-freeze-commit <FINAL_FREEZE_SHA>
```

当前严禁执行该命令、正式 `preflight` 或 `audit`。本端点的授权 SHA 为 `1fc48c1003c96e235ae123481669b650ba8f90d6`（短 SHA `1fc48c1`）；结果根已经存在并不可变。新的下游执行必须另获明确授权，不能用本授权或新提交替代。

### 8.8 旧正式分类控制器（当前禁止重跑）

```powershell
python scripts/run_iotj_canonical_v1_classification.py --help
python scripts/run_iotj_canonical_v1_comparators.py --help
python scripts/run_iotj_canonical_v1_a0t.py --help
python scripts/run_iotj_c5_label_budget.py --help
```

这些入口用于理解参数或新设计复用，不代表授权重新执行已有 experiment ID。

---

## 9. 结果、checkpoint 和审计约定

### 9.1 结果目录应包含什么

正式 study 通常至少应保存：

- protocol/locked run spec；
- experiment registry；
- checkpoint 或 ordered state fingerprint；
- raw prediction 或可验证的 compact record；
- primary metrics CSV；
- per-target/per-gas/per-scope metrics；
- loss-activity/label-access/target-field audit；
- runtime/communication cost；
- result analysis；
- strict audit；
- SHA256 index；
- completion marker 和 sealed-test-open record。

### 9.2 checkpoint 相等性

- 模型语义相等使用 **ordered state-content fingerprint**；
- whole-file SHA256 用于文件 provenance；
- 不要因为 PyTorch checkpoint 文件 SHA 不同就直接断定模型 state 不同；
- 不要因为文件名相同就断定模型 state 相同。

### 9.3 target 信息门

- target TEST label leakage 是绝对 hard fail；
- target CALIBRATION 信息按 method-specific contract 审计；
- CORAL/MMD/DANN canonical baseline 是 target `x only`；
- A4 可使用注册允许的 calibration x/class/phase；
- target concentration 不能悄悄进入分类 adaptation；
- target test 不能用于 loss、early stopping、threshold、hyperparameter 或 checkpoint selection。

### 9.4 Git 提交原则

- 只提交任务相关代码、文档和紧凑证据；
- checkpoint、大型 cache 和临时运行目录是否提交由 protocol 决定；
- 提交前查看 `git status --short` 和 `git diff --check`；
- 不要混入 watcher logs、无关临时 pytest 目录或用户正在编辑的文件；
- 推送目标：`origin/codex/iotj-final-classification-le1`。

当前工作树中存在历史 watcher log 修改和大量 `.tmp_pytest_*` 目录。它们不属于本次文档更新，不应清理、覆盖或误提交。

---

## 10. 已知局限

### 10.1 科学局限

- 许多正式结论只有 seed42，不能声称 multi-seed 稳定性。
- canonical target split 是 window-level 20/80；历史审计发现 calibration/test 可能存在 raw-file/raw-time 关系，不能声称严格 raw-file-disjoint 泛化。
- C5 low-label 研究保持 40/40 class×concentration strata，结论只适用于注册的 nested window budget。
- A4 分类优势相对 equal-label A0T 不稳定/很小，不能主张普遍分类优越性。
- source-DG 与 cost-aware routing 的注册验证未支持。
- 旧 R84/QC quantitative evidence 混入 legacy feature provenance，不能作为 canonical final quantitative claim。
- 原 canonical regression reconstruction 停在 R0 exact-recovery；独立 R0-v2 已 formal PASS，但只建立 source-only 的 FedRidge 数值/代数等价，不建立 bitwise identity、target/client performance、accuracy benefit 或 statistical inference。尚无新的 C3/C4/C5 canonical regression/QC 结果。

### 10.2 工程局限

- 正式拓扑依赖三台节点都可达，以及 Windows SSH tunnel 正常。
- Pi 局域网 IP 可能随网络变化。
- results 中有大量历史资产，文件名含 `final` 不保证仍是当前科学终点。
- 远端 `/root/GAPS` checkout 与正式 hash runtime 可能不同；以 locked archive/runtime provenance 为准。
- 系统 Python 和专用 venv 是不同环境，不能混用检查结果。

### 10.3 当前最大的证据缺口

```text
R0-v2 已在不复用 legacy 10 Hz/100×8 quantitative assets 的前提下，
判定 canonical 5 Hz/50×8 FedRidge 通过全部已冻结的可审计数值等价门。
当前最大缺口是尚未执行的 R1 及后续 QC necessity，而非 R0-v2 formal evidence。
```

近常量策略和数值 tolerance 已在 R0-v2 协议中冻结，且本端点不得重跑或由 runner 自动放宽；当前不应继续算法搜索。

---

## 11. 常见错误与正确处理

### 错误 1：看到没有 CUDA就准备本机长时间 CPU 重训

正确处理：先检查三机专用 venv 和冻结 CPU 拓扑。Windows 不承担两个 source client 的正式训练。

### 错误 2：在 121.40 的系统 Python 导入 torch 失败就判定服务器不可用

正确处理：检查 `/root/gaps_env/bin/python`。正式命令从不使用裸 `python`。

### 错误 3：看到结果目录不完整就从头重跑

正确处理：先读 registry、runner logs、completion marker 和 failure audit。已完成端点不可重跑，失败证据必须保留。

### 错误 4：把旧 `client_data_c1234src_c5tgt...` 当 canonical-v1

正确处理：当前正式输入只能是 `dataset/iotj_canonical_v1` 及其冻结 manifest/hash。旧 dataset 只用于 provenance 对照。

### 错误 5：把旧 A0T/A4 regression 的 `REGRESSION_ADVANTAGE_SUPPORTED` 直接写成最终 canonical 结论

正确处理：同时读取后续 `regression_qc_closure_20260811` hard fail 和当前 R0 状态；旧结果只作描述性 routing diagnostic。

### 错误 6：为让 R0 通过而改 tolerance 或删掉近常量特征

正确处理：先提出独立 protocol amendment、决策依据和新的 output root；未经授权保持 fail closed。

### 错误 7：用 target test 选择方法或 checkpoint

正确处理：test 仅在所有固定端点完成后一次性评价。任何提前访问都是 hard fail。

---

## 12. 给新对话的推荐交接提示词

可以把下面内容直接放到新对话第一条消息：

```text
请先只读检查并完整阅读当前工作树中的《代码文件介绍.md》，然后读取：
1) docs/experiments/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812/protocol_manifest.json
2) 同目录 PROTOCOL.md、EXPERIMENT_MATRIX.csv、EXPERIMENT_REGISTRY.csv 和数值策略文件
3) docs/experiments/iotj_canonical_v1_final/canonical_regression_reconstruction_qc_20260811/protocol_manifest.json
4) results/.../C0/C0_EXPERIMENT_AUDIT.md
5) results/.../R0/R0_FAILURE_AUDIT.json
6) results/.../R0/R0_NUMERICAL_FAILURE_ANALYSIS.md

当前分支是 codex/iotj-final-classification-le1，科学结果基线为 c6055ea。
C0 已完成并决定 V1_INTERLEAVED_RETAINED；原 R0 exact-recovery 已 fail closed 且必须原样保留。
独立 R0-v2 已在授权 1fc48c1 formal PASS，结果根
results/iotj_canonical_v1_final/canonical_fedridge_r0_v2_20260812 已存在且不可变。
R0-v2 严格只使用 C1/C2 source，不访问 C3/C4/C5 target bytes/labels，不运行 QC；
R1 已完成且为 `DEVICE_DEPENDENT`，R2 已触发但尚无结果，Q0/Q1 未开放。不要重跑已完成端点，不要修改 canonical-v1、target split、
A4、R84、QC、alpha、near-constant policy、数值 gate 或阈值，不要打开 target test，
不要自动继续算法搜索。

当前禁止重新运行 scripts/run_iotj_canonical_fedridge_r0_v2.py run、正式 preflight 或 audit。
R0-v2 已用授权 `1fc48c1` 完成；不要把该授权、文档更新或提交解释为 R1 或其他下游执行授权。

正式拓扑是 CPU 三机：
- server/DA root@121.40.139.213，/root/gaps_env/bin/python
- C1 Pi gaps@192.168.137.172，/home/gaps/GAPS/gaps_rpi_env/bin/python
- C2 root@114.55.171.63，/root/gaps_c2_cpu_env/bin/python
Windows 负责控制、评估和审计。不要因 Windows/121.40 无 CUDA或系统 Python 无 torch
而询问新的 CUDA 节点；先检查三机专用环境。

先汇报：原 R0 与 R0-v2 的独立状态、8-file protocol bundle、不可变 formal result root、
现有验证证据、工作树脏文件、远程可达性、R1 closure 状态、R2 尚无结果的缺口和下一步是否需要新授权。
未经我另行明确授权，不启动任何正式训练、R1 重跑或其他下游端点。
```

---

## 13. 本文维护规则

每次满足以下任一条件，都应同步更新本文并推送 GitHub：

- 当前 stage、decision 或 stop rule 改变；
- 新 experiment ID、manifest、registry 或 result root 建立；
- 远程 IP、repo、venv 或数据路径改变；
- 正式运行命令改变；
- 某项历史结果被 invalidated、superseded 或 promoted；
- 论文 claim 的证据级别改变；
- 新的 fail-closed 原因或恢复协议出现。

更新时必须：

1. 写清楚日期、分支和科学基线 commit；
2. 区分“完成”“失败”“阻塞”“未开始”“历史诊断”；
3. 给出实际 result/protocol 路径；
4. 给出可复制命令，但对已完成端点标注“禁止重跑”；
5. 不写密码、私钥、token；
6. 运行路径、UTF-8、Markdown、pytest/compileall 和 Git diff 检查；
7. commit 并 push 当前分支。

本文的目标不是保存每个历史实验的全部细节，而是让接手者在几分钟内知道：**现在在哪里、什么已经完成、什么被否定、什么不能做、代码在哪里、结果在哪里、三机如何连接、下一步为什么需要或不需要授权。**
