# GAPS 代码文件介绍与当前主线清单

最后更新：2026-07-13

本文档用于说明当前 GAPS 仓库里哪些代码文件仍然有用、哪些是主线入口、哪些更适合保留为诊断/归档。当前项目已经不再是早期单机 `exp_improved.py` 主线，而是以 **真实云边 Flower 分类训练 + C5 个性化回归校准 + 部署 runtime/QC** 为核心。

当前方法的完整代码对齐说明见：

```text
docs/paper/iotj_system_methodology_20260711.zh.md
```

实验事实、失败记录和下一步 gate 统一维护在：

```text
docs/experiments/iotj_system_experiment_notebook.md
```

## 0. 当前主线总览

当前冻结协议只允许 **C1/C2 source -> C5 target**。C3/C4 不作为主目标域，也不得进入 C5 主指标。当前系统可以拆成四层：

```text
time-aware 60-170 s C1/C2/C5 window dataset
-> causal Flower classification ablation on ECS + Pi + PC
-> C5 calibration-assisted server adaptation
-> C5 target-personalized regression and validation-only expert selection
-> deployment runtime and QC output
-> reports / evidence summaries
```

当前最重要的结论口径：

- 分类训练：Flower 客户端只训练分类骨干，明确设置 `USE_REG_LOSS=False`。主实验为 A0/A0T/A2/A3/A4/A4S/A5/A6/A7，不能再用一个旧 `strong_cls` 结果代替因果消融。
- 域适应：A5-A7 使用 C5 calibration 类别/阶段标签，应称为 calibration-assisted DA，不能称为无监督 DA。A0T 是相同目标标签预算的 target-CE 基线。
- 回归：R3aK16 仅为 C1/C2 source reference。C5 实际同时训练 Ridge 和浅层 MLP；H2.3+、固定 H8 和简单 CO gate 是正式候选。P4 必须先替换历史上使用 `true_class` 量程的泄漏风险，才能重新进入候选表。
- 主报告：先给分类性能和 QC 前的 `S_CC` 回归能力线，再给真实 predicted-route 的 `S_ALL` 与 QC 后 N/coverage 系统线。`S_AR intersect S_CC` 只是诊断。
- QC：QC 是部署可靠性层，不负责证明原始模型能力，也不能只报低误差而隐藏筛选覆盖率。
- C4 rescue：不属于 C5-only 主协议；所有旧 H8+C4/P4 C3-C5 材料只作历史诊断。
- 实现审查：上述 MMD 再平方、域内 stage-MMD 和 WGAN-GP+GRL 方向问题属于冻结的 v2 语义；v3 B1-B5 已提供 conventional MMD²、跨域同 class/phase 对齐和修正的 Wasserstein feature objective。`proto_mmd` 仍是无梯度 detached prototype pair-L2，v3 将其权重固定为 0。
- checkpoint：`use_adapted_as_global=true` 的 `server_latest.pth` 不是独立 no-DA 模型；no-DA 必须使用 A0/A2-A4 这类独立关闭服务器适应的运行。
- 结果状态：v2r1 A0-A7 与 v3 B1-B5 的 seed-42 真实 ECS/Pi/PC 筛选均已完成。B2 分类最好，accuracy/macro-F1 为 99.2647%/99.2657%；B5 是预声明完整组合但 accuracy 为 98.8971%。正式 ECS 回归已覆盖 A6/B5，并按同流程探索性重放 B2；B2 固定 H8 的 S_ALL/S_CC RMSE 为 14.6564/11.3288 ppm。B2 是在分类 test 排名打开后选择的，必须标为 post-screen 探索性结果。当前仍缺种子 43-46，不能用单种子排名替代均值与不确定性结论。
- 跨方向状态：已经冻结 B2/B5 的 C1->C5、C5->C1、C4/C5->C1 配对实验。它是简化方法的 appendix/generalization 验证，不替换 C1/C2->C5 主协议。六组 seed-42 manifests、真实设备控制器和配对统计均已完成；当前 ECS 正常，等待 Pi 重新接入 `192.168.31.0/24` 后启动真实训练。

## 1. 最推荐阅读顺序

第一次接手当前代码，建议按这个顺序读：

```text
README.md
docs/paper/iotj_system_methodology_20260711.zh.md
docs/superpowers/plans/2026-07-11-iotj-system-experiment-closure.md
docs/superpowers/specs/2026-07-13-b2-b5-cross-direction-classification.md
docs/superpowers/plans/2026-07-13-b2-b5-cross-direction-classification.md
docs/experiments/iotj_system_experiment_notebook.md
docs/mainline_entrypoints_20260626.md
docs/feature_schema_and_runtime_contract_20260626.md
scripts/generate_iotj_classification_ablation_commands.py
scripts/run_iotj_classification_cloud_edge.py
scripts/summarize_iotj_classification_ablation.py
scripts/generate_iotj_cross_direction_commands.py
scripts/run_iotj_cross_direction_cloud_edge.py
scripts/summarize_iotj_cross_direction_classification.py
gaps_flower/server_app.py
gaps_flower/client_app.py
gaps_flower/strategy.py
gaps_flower/domain_adaptation.py
gaps_flower/evaluate_checkpoint.py
scripts/build_iotj_c5_regression_inputs.py
scripts/run_iotj_c5_h23_plus.py
run_source_augmented_target_ridge_eval.py
scripts/select_iotj_c5_p4.py
gaps_deploy/final_runtime.py
gaps_deploy/rich_residual.py
gaps_deploy/qc_policy.py
```

## 2. 数据与基础模型文件

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `config.py` | 保留 | 全局配置、模型和训练参数集合。 |
| `model.py` | 必须保留 | TCN/backbone、分类头、R3aK16 等模型结构定义。 |
| `utils.py` | 必须保留 | 模型创建、归一化、评估和公共工具。 |
| `federated_dataset.py` | 必须保留 | 数据集/Dataloader 适配，支撑 Flower 和回归训练。 |
| `preprocessor_time_aware.py` | 保留 | time-aware 60-170 s 特征窗口生成相关入口。 |
| `split_dataset.py` | 保留但谨慎 | 数据划分和历史 split 逻辑；新实验必须确认 source/target role 是否正确。 |
| `scripts/audit_target_split.py` | 保留 | 检查目标域 calibration/test split 是否符合实验协议。 |
| `scripts/merge_target_splits.py` | 保留 | split 合并/整理工具。 |

旧的 `preprocessor.py` 仍可用于历史复现，但当前主线优先使用 time-aware 60-170 s window 数据。

## 3. Flower 分类训练主线

### 核心包：`gaps_flower/`

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_flower/task.py` | 必须保留 | Flower 配置、模型构建、数据加载、参数序列化。 |
| `gaps_flower/client_app.py` | 必须保留 | Flower client 入口，树莓派/PC source client 本地训练。 |
| `gaps_flower/server_app.py` | 必须保留 | Flower server 入口，阿里云端启动训练。 |
| `gaps_flower/strategy.py` | 必须保留 | GAPS strategy、FedAvg、prototype 统计、checkpoint 保存、DA hook。 |
| `gaps_flower/domain_adaptation.py` | 必须保留 | server-side fixed/strong DA：CORAL、MMD、prototype anchor、adversarial 等。 |
| `gaps_flower/evaluate_checkpoint.py` | 必须保留 | 对 `server_latest.pth` / `server_latest_adapted.pth` 做 target test 评估。 |

### 当前分类矩阵/远程训练相关脚本

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `scripts/generate_iotj_classification_ablation_commands.py` | 当前冻结命令入口 | 生成 C1/C2->C5 的 A0-A7、确认种子和条件 appendix manifests，并验证协议字段。 |
| `scripts/run_iotj_classification_cloud_edge.py` | 当前真实训练 controller | 检查 ECS 空闲，向 ECS/Pi 同步精确 runtime 和 manifests，启动 ECS server、Pi C1、PC C2 并恢复产物。 |
| `scripts/summarize_iotj_classification_ablation.py` | 当前分类评估入口 | 强制 final round 25，识别 A0T/A4S 等完整组 ID，输出 C5 calibration/test 行流、accuracy、macro-F1、recall、NLL、ECE 和混淆矩阵。 |
| `configs/iotj_b2_b5_cross_direction_20260713.json` | 跨方向冻结配置 | 固定 F1 C1->C5、R1 C5->C1、R2 C4/C5->C1 的数据根、行数、训练设备和 B2/B5 顺序。 |
| `scripts/generate_iotj_cross_direction_commands.py` | 跨方向命令入口 | 复用冻结的 v3 B2/B5 定义，校验 source train/source calibration/target calibration-test 行数与四类均衡，记录活动文件 SHA-256 并生成六组 manifests。 |
| `scripts/run_iotj_cross_direction_cloud_edge.py` | 跨方向真实训练 controller | 按 manifest 动态启动 Pi-only 或 Pi+PC 源客户端；ECS 只同步 source/target calibration，边缘端只同步各自 source train；拒绝覆盖部分远端运行并审计 25 轮恢复产物。 |
| `scripts/summarize_iotj_cross_direction_classification.py` | 跨方向配对评估入口 | 从 manifest 读取目标客户端，输出 C1/C5 逐窗口概率、Accuracy/Macro-F1/NLL/ECE，并做严格同行配对、McNemar 和分层 bootstrap。 |
| `scripts/audit_iotj_experiment_inputs.py` | 必须保留 | 冻结 C1/C2/C5 数据角色、C5 320/1360 行数、输入哈希和历史 F2 完整性。 |
| `scripts/remote_launch_flower_server_clean.py` | 必须保留 | 阿里云端安全启动 Flower server，避免 Windows OpenSSH 引号问题。 |
| `scripts/remote_launch_flower_client_clean.py` | 必须保留 | 树莓派端安全启动 Flower client。 |
| `scripts/evaluate_clean_matrix_remote.py` | 历史矩阵复核 | 对旧 clean matrix checkpoint 做 target test 评估；不能替代当前 v2 消融。 |
| `scripts/run_source_target_matrix_pi_controller_20260707_clean.ps1` | 历史矩阵入口 | F4/F5/R1-R4 clean run controller，不是当前 C5-only 主训练入口。 |
| `scripts/run_fixed_da_source_target_experiment.py` | 测试/历史复现 | 可做本地代码 smoke，但本地模拟结果不能进入论文性能表。 |
| `generate_flower_matrix_commands.py` | 历史辅助 | 生成旧 source-target matrix 命令/manifest。 |
| `summarize_flower_matrix_classification.py` | 历史辅助 | 汇总旧分类矩阵结果。 |
| `summarize_comm_compression_classification.py` | 辅助保留 | 通信压缩实验汇总。 |

当前冻结命令与 controller 记录：

```text
results/iotj_classification_ablation_20260711_v2_commands/
results/iotj_classification_ablation_20260711_v2r1_controller/
results/iotj_classification_ablation_20260711_v2r1/
results/iotj_classification_ablation_20260711_v2r1_summary/
results/iotj_b2_b5_cross_direction_20260713_commands/
```

当前阿里云运行目录：

```text
/root/GAPS/results/iotj_classification_ablation_20260711_v2r1/
```

旧 F1-F4 source-count matrix 只可作为 C5 generalization 的补充证据。F5 和 reverse R1-R4 不符合主协议，不进入主结果。

## 4. 回归与目标端校准主线

### R3aK16/source regression base

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_flower/regression_task.py` | 必须保留 | 构建回归模型、source regression 训练/FedAvg 支撑逻辑。 |
| `gaps_flower/regression_client.py` | 保留/参考 | 文件式 source client 回归训练入口。 |
| `gaps_flower/regression_server.py` | 保留/参考 | source regression checkpoint FedAvg 聚合入口。 |
| `run_regression_head_ablation.py` | 保留 | 回归头/特征消融和轻量化实验工具。 |

R3aK16 当前定位：

```text
source-domain neural regression base/reference
```

不要把它表述成最终最优 ppm 输出。目标端 H2.3/H8 等校准/直连头才是当前性能主线。

### C5 target Ridge / MLP / H2.3+ / H8 / selector

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `scripts/build_iotj_c5_regression_inputs.py` | 当前统一输入入口 | 把确认后的 C5 分类行流、backbone 特征和 C1/C2 source reference 按键对齐为 320/1360 行。 |
| `run_formal_target_ridge_auto_v2_eval.py` | 必须保留 | C5 per-gas target Ridge，calibration-validation 选 alpha，完整 calibration 重拟合。 |
| `run_formal_target_mlp_auto_v2_eval.py` | 必须保留 | C5 per-gas 浅层 MLP，搜索 hidden/alpha。 |
| `scripts/run_iotj_c5_h23_plus.py` | 当前 H2.3+ 入口 | C5 MLP anchor 与 rich+reg-feature Ridge 受约束融合，不含 C4 rescue。 |
| `run_h2_3_plus_fusion_profile.py` | 必须保留 | Ridge/MLP family、融合权重和 non-CO guard 的共享实现。 |
| `run_source_augmented_target_ridge_eval.py` | 当前 H8 入口 | C1/C2 source Ridge/MLP/shared-MLP 预测增强 C5 target Ridge；主实验必须传 `--disable-c4-rescue`。 |
| `scripts/select_iotj_c5_p4.py` | 当前专家消融入口 | 在 calibration-validation 上比较 H2.3+、H8、simple CO gate、P4 和 oracle diagnostic。 |
| `run_final_metric_consolidation_20260709.py` | 当前切片合同 | 独立输出 S_ALL、S_CC、S_CW、S_AR 和诊断交集。 |
| `run_formal_c4_route_rescue_selector.py` | 历史诊断 | 旧 C4 route-rescue gate，不属于 C5-only 主协议。 |
| `run_formal_c4_rescue_auto_v2_eval.py` | 历史诊断 | 旧 C4 rescue formal 评估。 |
| `summarize_bidirectional_profile_selection.py` | 历史汇总 | 旧 C12->C345/C45->C123 双向汇总，不进入当前主表。 |

当前候选角色：

| Profile | 当前角色 |
|---|---|
| C5 per-gas Ridge | 经典目标校准基线 |
| H2.3 anchor | C5 per-gas expanded-grid MLP |
| H2.3+ | MLP anchor 与 weak reg-feature Ridge 的 validation-only 受约束融合 |
| H8 | C1/C2 source predictions 增强的 C5 per-gas Ridge，当前重要候选 |
| simple CO gate | predicted CO 使用 H8，其余使用 H2.3+ 的低复杂度基线 |
| P4 | 历史风险含 `true_class` 量程泄漏，当前只作失效诊断；重建 deployment-visible risk 后才可重新消融 |
| oracle expert | 仅作不可部署上界诊断 |

当前不能写“目标域只使用 Ridge”：H2.3/H2.3+ 明确训练 C5 浅层 MLP，H8 的最终目标头是 Ridge，且 H8 还使用 C1/C2 训练的 Ridge、per-gas MLP 和 shared MLP 预测作为输入。

### 轻量化与诊断实验

这些文件有参考价值，但不建议放进默认主线：

```text
run_source_lightweight_regression_head_ablation.py
run_source_lightweight_target_calibrated_eval.py
run_source_lightweight_full_auto_v2_eval.py
run_lightweight_l2_unified_selector.py
run_l3_lightweight_hybrid_matrix.py
run_low_calib_stress_profiles.py
run_low_calibration_blend_stress.py
run_low_calibration_profile_choice_stress.py
```

历史结论口径：轻量 source head 在加入目标端校准后可用，但旧协议中的部署证据不充分。当前 C5-only 论文只把这些文件作为 H8 source-prediction components 和轻量化诊断；L1 不能直接叫 deployment-lite。

## 5. 部署 runtime 与 package

注意：仓库当前已有的 H8+C4 bundle 属于旧 C3/C4/C5 协议的 runtime 证据。新的 C5-only 最终策略尚未冻结，也尚未接入 `final_runtime.py`；只有通过 calibration-validation 选择、C5 test 锁定评估和全 1360 行 runtime parity 后，才能替换 final bundle。当前 QC 缺失匹配 policy/风险字段时可能 fail-open，最终 bundle validator 必须把这些产物设为强制项。

### `gaps_deploy/` 主线文件

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_deploy/final_runtime.py` | 必须保留 | 最终部署 runtime，统一输出 schema，包含 `auto_output_ppm`。 |
| `gaps_deploy/rich_residual.py` | 必须保留 | H2.3/H8 及历史 H8+C4 rich residual/direct-head runtime policy。 |
| `gaps_deploy/inference.py` | 必须保留 | 基础部署 predictor：分类→回归→校准→QC。 |
| `gaps_deploy/deploy_config.py` | 必须保留 | 部署配置读取和路径管理。 |
| `gaps_deploy/calibration.py` | 必须保留 | bias/affine/full/specialist 等校准逻辑。 |
| `gaps_deploy/qc_policy.py` | 必须保留 | risk score 与 accept/review/reject 决策。 |
| `gaps_deploy/build_package.py` | 保留 | 构建部署包基础工具。 |
| `gaps_deploy/build_per_client_packages.py` | 保留 | 多 client 部署包构建。 |
| `gaps_deploy/validate_deployment_packages.py` | 保留 | 部署包结构预检。 |
| `gaps_deploy/predict_client_file.py` | 保留 | 文件级批量推理入口。 |

### 部署导出、验证与 benchmark

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `export_h8_formal_c4_rescue_deployment_candidate.py` | 历史部署证据 | 导出旧 H8+C4 v2 candidate；不作为当前 C5-only 最终包。 |
| `export_h8_source_aug_deployment_candidate.py` | 保留 | H8 source-aug bundle 导出。 |
| `export_target_ridge_profile_artifact.py` | 保留 | target Ridge profile artifact 导出。 |
| `validate_rich_residual_runtime_candidate.py` | 必须保留 | runtime vs analysis parity 验证。 |
| `compare_h8_runtime_equivalence.py` | 必须保留 | H8 runtime 等价性检查。 |
| `audit_h8_c4_guardrails.py` | 历史审计 | C4 rescue gate false hit / nonCO guardrail audit。 |
| `validate_feature_schema.py` | 必须保留 | feature/runtime schema 验证。 |
| `benchmark_runtime_profiles.py` | 必须保留 | bundle size / CPU latency benchmark。 |
| `scripts/build_final_deployment_package.py` | 保留 | final deployment bundle 构建入口。 |
| `scripts/validate_final_deployment_bundle.py` | 保留 | final deployment bundle 预检入口。 |

部署输出字段当前应使用：

```text
gas_class
gas_name
class_prob
base_r3ak16_raw_ppm
routed_pred_ppm
final_ppm
co_corrected_ppm
auto_output_ppm
qc_decision
risk_score
```

其中 `auto_output_ppm` 只在 `qc_decision == accept` 时作为系统自动输出；`final_ppm` 和 `co_corrected_ppm` 保留用于审计。

## 6. QC、可靠性与报告

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_deploy/build_qc_policy_from_predictions.py` | 保留 | 从 calibration prediction 构建 QC policy。 |
| `gaps_deploy/run_qc_guardrail_validation.py` | 保留 | QC guardrail 验证。 |
| `gaps_deploy/evaluate_qc_status_metrics.py` | 保留 | accept/review/reject 后指标统计。 |
| `summarize_qc_posthoc_profiles.py` | 保留 | post-hoc QC profile 汇总。 |
| `run_profile_qc_coverage_audit.py` | 保留 | QC 覆盖率和可靠性审计。 |
| `scripts/evaluate_coverage_review_qc.py` | 保留 | coverage/review 口径评估。 |

报告/证据打包相关：

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `run_current_base_evidence_freeze.py` | 保留 | 当前证据冻结包。 |
| `run_current_base_method_story.py` | 保留 | 方法故事线材料生成。 |
| `run_current_base_paper_story_pack.py` | 保留 | 论文/报告资料包。 |
| `run_final_metric_consolidation_20260709.py` | 保留 | 最终指标整合。 |
| `scripts/summarize_iotj_classification_ablation.py` | 当前分类表入口 | 生成 C5-only 分类行流、单次指标和多种子 mean/std。 |
| `docs/paper/iotj_system_methodology_20260711.zh.md` | 当前方法底稿 | 系统原理、公式、参数、实现映射、主张边界和审查风险。 |
| `docs/experiments/iotj_system_experiment_notebook.md` | 必须持续更新 | 记录命令、输入、输出、失败、决定和下一步 gate。 |
| `summarize_final_gaps_evidence.py` | 保留 | GAPS 证据汇总。 |
| `scripts/build_meeting_report_visuals.py` | 保留 | 会议图表生成。 |

## 7. 历史/归档/不建议作为主入口

这些文件并非没用，但不建议作为当前主线入口：

```text
client.py
server.py
exp_improved.py
adaptation.py
preprocessor.py
route_aware_response_anchoring.py
train_flower_local_fedavg.py
train_simplified_classifier_baseline.py
run_time_aware_split_backbone_gain_isolation.py
run_weighted_regression_loss_ablation.py
run_co_specific_correction_eval.py
run_co_specific_client_extension_eval.py
run_formal_c4_route_rescue_selector.py
run_formal_c4_rescue_auto_v2_eval.py
export_h8_formal_c4_rescue_deployment_candidate.py
run_c5_*_eval.py
codex_web_profile_reset.ps1
codex_force_web_profile_reset.ps1
codex_shell_proxy_setup.ps1
Start-Codex-With-Shell-Proxy.cmd
_local_archive_20260626/
历史版本代码备份归档/
```

其中 `client.py` 仍被 Flower task 复用本地损失实现，不能直接删除；早期 `server.py/exp_improved.py` 主要是历史参考。当前云边端入口应优先看 `gaps_flower/` 和 `scripts/run_iotj_classification_cloud_edge.py`。大量旧 `run_c5_*`、`diagnose_*`、`audit_*` 文件属于探索性诊断，适合保留但不应放进论文主流程图。

## 8. 当前有用代码文件最小清单

如果只保留“能复现当前主线”的最小代码集合，建议至少包括：

```text
config.py
model.py
utils.py
federated_dataset.py
preprocessor_time_aware.py
split_dataset.py

gaps_flower/task.py
gaps_flower/client_app.py
gaps_flower/server_app.py
gaps_flower/strategy.py
gaps_flower/domain_adaptation.py
gaps_flower/evaluate_checkpoint.py
gaps_flower/regression_task.py
gaps_flower/calibration_fit.py
gaps_flower/specialist_calibration_fit.py

gaps_deploy/final_runtime.py
gaps_deploy/rich_residual.py
gaps_deploy/inference.py
gaps_deploy/deploy_config.py
gaps_deploy/calibration.py
gaps_deploy/qc_policy.py
gaps_deploy/build_package.py
gaps_deploy/build_per_client_packages.py
gaps_deploy/validate_deployment_packages.py
gaps_deploy/predict_client_file.py

scripts/generate_iotj_classification_ablation_commands.py
scripts/run_iotj_classification_cloud_edge.py
scripts/summarize_iotj_classification_ablation.py
scripts/audit_iotj_experiment_inputs.py
scripts/remote_launch_flower_server_clean.py
scripts/remote_launch_flower_client_clean.py
scripts/audit_target_split.py
scripts/merge_target_splits.py
scripts/build_final_deployment_package.py
scripts/validate_final_deployment_bundle.py
scripts/build_iotj_c5_regression_inputs.py
scripts/run_iotj_c5_h23_plus.py
scripts/select_iotj_c5_p4.py

run_formal_target_ridge_auto_v2_eval.py
run_formal_target_mlp_auto_v2_eval.py
run_h2_3_plus_fusion_profile.py
run_source_augmented_target_ridge_eval.py
validate_rich_residual_runtime_candidate.py
compare_h8_runtime_equivalence.py
validate_feature_schema.py
benchmark_runtime_profiles.py
run_final_metric_consolidation_20260709.py

docs/paper/iotj_system_methodology_20260711.zh.md
docs/superpowers/plans/2026-07-11-iotj-system-experiment-closure.md
docs/experiments/iotj_system_experiment_notebook.md
```

## 9. 文件管理建议

当前 Git 工作区里有大量未跟踪的实验脚本和结果辅助脚本。建议按三类处理：

1. 主线必须保留并提交：`gaps_flower/`、`gaps_deploy/`、`scripts/remote_launch_*`、`scripts/evaluate_clean_matrix_remote.py`、最终汇总/验证脚本。
2. 诊断脚本保留但集中到 `diagnostics/` 或 `experiments/`：`diagnose_*`、`audit_*`、`run_c5_*`、`run_low_calibration_*`。
3. 本地工具或历史材料归档：Codex profile reset 脚本、旧单机 smoke、历史版本代码备份。

不要提交大体积数据和结果：

```text
dataset/
results/大型中间文件
deployment bundle
checkpoint .pth
临时日志
```

如果需要共享结果，优先提交轻量 summary：

```text
results/*summary*.md
results/*metrics*.csv
docs/*.md
```

## 18. 2026-07-13 正式回归闭环入口

当前有效的正式回归证据目录是：

```text
results/iotj_c5_formal_regression_20260713_v2
results/iotj_c5_formal_regression_20260713_v2_summary
```

不带 `_v2` 的 `results/iotj_c5_formal_regression_20260713` 仅保留为失败审查记录：其第一版统一流缺失 R1 rich-only Ridge 列，不能用于论文指标。

| 文件 | 当前作用 |
|---|---|
| `scripts/run_iotj_c5_regression_cloud.py` | 阿里云 ECS 正式回归同步、顺序执行、完整性检查和结果回收。 |
| `scripts/run_iotj_c5_regression_suite.py` | 串联 C5 输入、H2.3+、无 C4 rescue 的 H8、QC 和 R0-R7。 |
| `scripts/assemble_iotj_c5_regression_ladder.py` | 生成 R0-R7，R6 只用 calibration-validation 选阈值，R7 明确为离线 oracle。 |
| `scripts/evaluate_iotj_high_coverage_qc.py` | 生成 FULL、HC95、HC90、固定覆盖率排序诊断和 1000 次随机拒绝对照。 |
| `scripts/summarize_iotj_c5_formal_regression.py` | 校验并汇总 A6/B5/B2 的 168 行回归指标和 9 行 QC 指标，记录源文件哈希。 |

正式 seed-42 预声明观察：B5 固定 H8 的 S_ALL RMSE 为 `17.4473`，S_CC N=`1345`、RMSE=`11.3890`；B5-HC95 实际自动覆盖率为 `96.25%`，accepted RMSE 为 `15.9075`。探索性 B2 重放的固定 H8 S_ALL RMSE 为 `14.6564`，S_CC N=`1350`、RMSE=`11.3288`；B2-HC95 自动覆盖率为 `95.66%`，accepted RMSE 为 `12.6729`，并标记 `7/10` 个错路由窗口。B2 的更好 S_ALL 主要来自更少的分类错误，仍需种子 43-46 配对确认，不能直接表述为稳定显著提升。
