# GAPS 代码文件介绍与当前主线清单

最后更新：2026-07-17

本文档用于说明当前 GAPS 仓库里哪些代码文件仍然有用、哪些是主线入口、哪些更适合保留为诊断/归档。当前项目已经不再是早期单机 `exp_improved.py` 主线，而是以 **真实云边 Flower 分类训练 + C5 个性化回归校准 + 部署 runtime/QC** 为核心。

当前方法的完整代码对齐说明见：

```text
docs/paper/iotj_system_methodology_20260711.zh.md
```

新对话/GitHub 接手的统一状态入口：

```text
docs/experiments/iotj_latest_handoff_20260715.zh.md
```

实验事实、失败记录和下一步 gate 统一维护在：

```text
docs/experiments/iotj_system_experiment_notebook.md
```

2026-07-14 导师进展汇报稿及其可编辑来源：

```text
docs/paper/GAPS_IoTJ_advisor_progress_report_20260713.zh.docx
docs/paper/GAPS_IoTJ_advisor_progress_report_20260713.zh.md
```

## 0. 当前主线总览

当前冻结协议只允许 **C1/C2 source -> C5 target**。C3/C4 不作为主目标域，也不得进入 C5 主指标。当前系统可以拆成四层：

```text
time-aware 60-170 s C1/C2/C5 window dataset
-> causal Flower classification ablation on ECS + Pi + PC
-> C5 calibration-assisted server adaptation
-> C5 target-personalized regression and validation-only expert selection
-> deployment runtime and QC output
-> reports / evidence summaries
```

当前最重要的结论口径：

- 分类训练：Flower 客户端只训练分类骨干，明确设置 `USE_REG_LOSS=False`。主实验为 A0/A0T/A2/A3/A4/A4S/A5/A6/A7，不能再用一个旧 `strong_cls` 结果代替因果消融。
- 域适应：A5-A7 使用 C5 calibration 类别/阶段标签，应称为 calibration-assisted DA，不能称为无监督 DA。A0T 是相同目标标签预算的 target-CE 基线。
- 回归：R3aK16 仅为 C1/C2 source reference。C5 实际同时训练 Ridge 和浅层 MLP；H2.3+、固定 H8 和简单 CO gate 是正式候选。P4 必须先替换历史上使用 `true_class` 量程的泄漏风险，才能重新进入候选表。
- 主报告：先给分类性能和 QC 前的 `S_CC` 回归能力线，再给真实 predicted-route 的 `S_ALL` 与 QC 后 N/coverage 系统线。`S_AR intersect S_CC` 只是诊断。
- QC：QC 是部署可靠性层，不负责证明原始模型能力，也不能只报低误差而隐藏筛选覆盖率。
- C4 rescue：不属于 C5-only 主协议；所有旧 H8+C4/P4 C3-C5 材料只作历史诊断。
- 实现审查：上述 MMD 再平方、域内 stage-MMD 和 WGAN-GP+GRL 方向问题属于冻结的 v2 语义；v3 B1-B5 已提供 conventional MMD²、跨域同 class/phase 对齐和修正的 Wasserstein feature objective。`proto_mmd` 仍是无梯度 detached prototype pair-L2，v3 将其权重固定为 0。
- checkpoint：`use_adapted_as_global=true` 的 `server_latest.pth` 不是独立 no-DA 模型；no-DA 必须使用 A0/A2-A4 这类独立关闭服务器适应的运行。
- 结果状态：v2r1 A0-A7 与 v3 B1-B5 的 seed-42 真实 ECS/Pi/PC 筛选均已完成。B2 分类最好，accuracy/macro-F1 为 99.2647%/99.2657%；B5 是预声明完整组合但 accuracy 为 98.8971%。正式 ECS 回归已覆盖 A6/B5，并按同流程探索性重放 B2；B2 固定 H8 的 S_ALL/S_CC RMSE 为 14.6564/11.3288 ppm。B2 是在分类 test 排名打开后选择的，必须标为 post-screen 探索性结果。当前仍缺种子 43-46，不能用单种子排名替代均值与不确定性结论。
- 跨方向状态：B2/B5 的 C1->C5、C5->C1、C4+C5->C1 六组 seed-42 真实设备实验均已完成 25 轮、回收并按同窗口配对评估。B2/B5 accuracy 分别为 98.8971%/98.3088%、97.6493%/98.3582%、98.9552%/99.1418%；对应 exact McNemar p 为 0.0963、0.0043、0.3323。结论是 B2 在 C1->C5 数值更优，B5 在 C5->C1 显著更优，并在双源 C4+C5->C1 数值更优但不显著；完整适配栈的作用具有方向和源异质性依赖，不能写成普遍叠加增益或普遍冗余。该实验只作为 appendix/generalization 证据，不替换 C1/C2->C5 主协议，且仍需种子 43-46。完整六行表见 `docs/experiments/iotj_b2_b5_cross_direction_results_20260715.zh.md`。

## 1. 最推荐阅读顺序

第一次接手当前代码，建议按这个顺序读：

```text
docs/experiments/iotj_latest_handoff_20260715.zh.md
代码文件介绍.md
docs/experiments/iotj_system_experiment_notebook.md
docs/paper/iotj_system_methodology_20260711.zh.md
docs/paper/GAPS_IoTJ_paper_draft_20260711.zh.md
docs/experiments/iotj_evidence_archive_20260714.md
README.md
docs/paper/GAPS_IoTJ_advisor_progress_report_20260713.zh.md
docs/superpowers/plans/2026-07-11-iotj-system-experiment-closure.md
docs/superpowers/specs/2026-07-13-b2-b5-cross-direction-classification.md
docs/superpowers/plans/2026-07-13-b2-b5-cross-direction-classification.md
docs/mainline_entrypoints_20260626.md
docs/feature_schema_and_runtime_contract_20260626.md
scripts/generate_iotj_classification_ablation_commands.py
scripts/run_iotj_classification_cloud_edge.py
scripts/summarize_iotj_classification_ablation.py
scripts/generate_iotj_cross_direction_commands.py
scripts/run_iotj_cross_direction_cloud_edge.py
scripts/summarize_iotj_cross_direction_classification.py
gaps_flower/server_app.py
gaps_flower/client_app.py
gaps_flower/strategy.py
gaps_flower/domain_adaptation.py
gaps_flower/evaluate_checkpoint.py
scripts/build_iotj_c5_regression_inputs.py
scripts/run_iotj_c5_h23_plus.py
run_source_augmented_target_ridge_eval.py
scripts/select_iotj_c5_p4.py
gaps_deploy/final_runtime.py
gaps_deploy/rich_residual.py
gaps_deploy/qc_policy.py
```

## 2. 数据与基础模型文件

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `config.py` | 保留 | 全局配置、模型和训练参数集合。 |
| `model.py` | 必须保留 | TCN/backbone、分类头、R3aK16 等模型结构定义。 |
| `utils.py` | 必须保留 | 模型创建、归一化、评估和公共工具。 |
| `federated_dataset.py` | 必须保留 | 数据集/Dataloader 适配，支撑 Flower 和回归训练。 |
| `preprocessor_time_aware.py` | 保留 | time-aware 60-170 s 特征窗口生成相关入口。 |
| `split_dataset.py` | 保留但谨慎 | 数据划分和历史 split 逻辑；新实验必须确认 source/target role 是否正确。 |
| `scripts/audit_target_split.py` | 保留 | 检查目标域 calibration/test split 是否符合实验协议。 |
| `scripts/merge_target_splits.py` | 保留 | split 合并/整理工具。 |

旧的 `preprocessor.py` 仍可用于历史复现，但当前主线优先使用 time-aware 60-170 s window 数据。

## 3. Flower 分类训练主线

### 核心包：`gaps_flower/`

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_flower/task.py` | 必须保留 | Flower 配置、模型构建、数据加载、参数序列化。 |
| `gaps_flower/client_app.py` | 必须保留 | Flower client 入口，树莓派/PC source client 本地训练。 |
| `gaps_flower/server_app.py` | 必须保留 | Flower server 入口，阿里云端启动训练。 |
| `gaps_flower/strategy.py` | 必须保留 | GAPS strategy、FedAvg、prototype 统计、checkpoint 保存、DA hook。 |
| `gaps_flower/domain_adaptation.py` | 必须保留 | server-side fixed/strong DA：CORAL、MMD、prototype anchor、adversarial 等。 |
| `gaps_flower/evaluate_checkpoint.py` | 必须保留 | 对 `server_latest.pth` / `server_latest_adapted.pth` 做 target test 评估。 |

### 当前分类矩阵/远程训练相关脚本

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `scripts/generate_iotj_classification_ablation_commands.py` | 当前冻结命令入口 | 生成 C1/C2->C5 的 A0-A7、确认种子和条件 appendix manifests，并验证协议字段。 |
| `scripts/run_iotj_classification_cloud_edge.py` | 当前真实训练 controller | 检查 ECS 空闲，向 ECS/Pi 同步精确 runtime 和 manifests，启动 ECS server、Pi C1、PC C2 并恢复产物。 |
| `scripts/summarize_iotj_classification_ablation.py` | 当前分类评估入口 | 强制 final round 25，识别 A0T/A4S 等完整组 ID，输出 C5 calibration/test 行流、accuracy、macro-F1、recall、NLL、ECE 和混淆矩阵。 |
| `configs/iotj_b2_b5_cross_direction_20260713.json` | 跨方向冻结配置 | 固定 F1 C1->C5、R1 C5->C1、R2 C4/C5->C1 的数据根、行数、训练设备和 B2/B5 顺序。 |
| `scripts/generate_iotj_cross_direction_commands.py` | 跨方向命令入口 | 复用冻结的 v3 B2/B5 定义，校验 source train/source calibration/target calibration-test 行数与四类均衡，记录活动文件 SHA-256 并生成六组 manifests。 |
| `scripts/run_iotj_cross_direction_cloud_edge.py` | 跨方向真实训练 controller | 按 manifest 动态启动 Pi-only 或 Pi+PC 源客户端；ECS 只同步 source/target calibration，边缘端只同步各自 source train；拒绝覆盖部分远端运行并审计 25 轮恢复产物。 |
| `scripts/summarize_iotj_cross_direction_classification.py` | 跨方向配对评估入口 | 从 manifest 读取目标客户端，输出 C1/C5 逐窗口概率、Accuracy/Macro-F1/NLL/ECE，并做严格同行配对、McNemar 和分层 bootstrap。 |
| `scripts/audit_iotj_experiment_inputs.py` | 必须保留 | 冻结 C1/C2/C5 数据角色、C5 320/1360 行数、输入哈希和历史 F2 完整性。 |
| `scripts/remote_launch_flower_server_clean.py` | 必须保留 | 阿里云端安全启动 Flower server，避免 Windows OpenSSH 引号问题。 |
| `scripts/remote_launch_flower_client_clean.py` | 必须保留 | 树莓派端安全启动 Flower client。 |
| `scripts/evaluate_clean_matrix_remote.py` | 历史矩阵复核 | 对旧 clean matrix checkpoint 做 target test 评估；不能替代当前 v2 消融。 |
| `scripts/run_source_target_matrix_pi_controller_20260707_clean.ps1` | 历史矩阵入口 | F4/F5/R1-R4 clean run controller，不是当前 C5-only 主训练入口。 |
| `scripts/run_fixed_da_source_target_experiment.py` | 测试/历史复现 | 可做本地代码 smoke，但本地模拟结果不能进入论文性能表。 |
| `generate_flower_matrix_commands.py` | 历史辅助 | 生成旧 source-target matrix 命令/manifest。 |
| `summarize_flower_matrix_classification.py` | 历史辅助 | 汇总旧分类矩阵结果。 |
| `summarize_comm_compression_classification.py` | 辅助保留 | 通信压缩实验汇总。 |

当前冻结命令与 controller 记录：

```text
results/iotj_classification_ablation_20260711_v2_commands/
results/iotj_classification_ablation_20260711_v2r1_controller/
results/iotj_classification_ablation_20260711_v2r1/
results/iotj_classification_ablation_20260711_v2r1_summary/
results/iotj_b2_b5_cross_direction_20260713_commands/
```

当前阿里云运行目录：

```text
/root/GAPS/results/iotj_classification_ablation_20260711_v2r1/
```

旧 F1-F4 source-count matrix 只可作为 C5 generalization 的补充证据。F5 和 reverse R1-R4 不符合主协议，不进入主结果。

## 4. 回归与目标端校准主线

### R3aK16/source regression base

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_flower/regression_task.py` | 必须保留 | 构建回归模型、source regression 训练/FedAvg 支撑逻辑；本地 checkpoint 固化训练时 `n_samples`。 |
| `gaps_flower/regression_client.py` | 保留/参考 | 文件式 source client 回归训练入口。 |
| `gaps_flower/regression_server.py` | 保留/参考 | source regression checkpoint FedAvg 聚合入口；以 checkpoint `n_samples` 为权重，当前数据只可选作一致性核对。 |
| `run_regression_head_ablation.py` | 保留 | 回归头/特征消融和轻量化实验工具。 |

R3aK16 当前定位：

```text
source-domain neural regression base/reference
```

不要把它表述成最终最优 ppm 输出。目标端 H2.3/H8 等校准/直连头才是当前性能主线。

### C5 target Ridge / MLP / H2.3+ / H8 / selector

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `scripts/build_iotj_c5_regression_inputs.py` | 当前统一输入入口 | 把确认后的 C5 分类行流、backbone 特征和 C1/C2 source reference 按键对齐为 320/1360 行。 |
| `run_formal_target_ridge_auto_v2_eval.py` | 必须保留 | C5 per-gas target Ridge，calibration-validation 选 alpha，完整 calibration 重拟合。 |
| `run_formal_target_mlp_auto_v2_eval.py` | 必须保留 | C5 per-gas 浅层 MLP，搜索 hidden/alpha。 |
| `scripts/run_iotj_c5_h23_plus.py` | 当前 H2.3+ 入口 | C5 MLP anchor 与 rich+reg-feature Ridge 受约束融合，不含 C4 rescue。 |
| `run_h2_3_plus_fusion_profile.py` | 必须保留 | Ridge/MLP family、融合权重和 non-CO guard 的共享实现。 |
| `run_source_augmented_target_ridge_eval.py` | 当前 H8 入口 | C1/C2 source Ridge/MLP/shared-MLP 预测增强 C5 target Ridge；主实验必须传 `--disable-c4-rescue`。 |
| `scripts/select_iotj_c5_p4.py` | 当前专家消融入口 | 在 calibration-validation 上比较 H2.3+、H8、simple CO gate、P4 和 oracle diagnostic。 |
| `run_final_metric_consolidation_20260709.py` | 当前切片合同 | 独立输出 S_ALL、S_CC、S_CW、S_AR 和诊断交集。 |
| `run_formal_c4_route_rescue_selector.py` | 历史诊断 | 旧 C4 route-rescue gate，不属于 C5-only 主协议。 |
| `run_formal_c4_rescue_auto_v2_eval.py` | 历史诊断 | 旧 C4 rescue formal 评估。 |
| `summarize_bidirectional_profile_selection.py` | 历史汇总 | 旧 C12->C345/C45->C123 双向汇总，不进入当前主表。 |

当前候选角色：

| Profile | 当前角色 |
|---|---|
| C5 per-gas Ridge | 经典目标校准基线 |
| H2.3 anchor | C5 per-gas expanded-grid MLP |
| H2.3+ | MLP anchor 与 weak reg-feature Ridge 的 validation-only 受约束融合 |
| H8 | C1/C2 source predictions 增强的 C5 per-gas Ridge，当前重要候选 |
| simple CO gate | predicted CO 使用 H8，其余使用 H2.3+ 的低复杂度基线 |
| P4 | 历史风险含 `true_class` 量程泄漏，当前只作失效诊断；重建 deployment-visible risk 后才可重新消融 |
| oracle expert | 仅作不可部署上界诊断 |

当前不能写“目标域只使用 Ridge”：H2.3/H2.3+ 明确训练 C5 浅层 MLP，H8 的最终目标头是 Ridge，且 H8 还使用 C1/C2 训练的 Ridge、per-gas MLP 和 shared MLP 预测作为输入。

### 轻量化与诊断实验

这些文件有参考价值，但不建议放进默认主线：

```text
run_source_lightweight_regression_head_ablation.py
run_source_lightweight_target_calibrated_eval.py
run_source_lightweight_full_auto_v2_eval.py
run_lightweight_l2_unified_selector.py
run_l3_lightweight_hybrid_matrix.py
run_low_calib_stress_profiles.py
run_low_calibration_blend_stress.py
run_low_calibration_profile_choice_stress.py
```

历史结论口径：轻量 source head 在加入目标端校准后可用，但旧协议中的部署证据不充分。当前 C5-only 论文只把这些文件作为 H8 source-prediction components 和轻量化诊断；L1 不能直接叫 deployment-lite。

## 5. 部署 runtime 与 package

注意：仓库当前已有的 H8+C4 bundle 属于旧 C3/C4/C5 协议的 runtime 证据。新的 C5-only 最终策略尚未冻结，也尚未接入 `final_runtime.py`；只有通过 calibration-validation 选择、C5 test 锁定评估和全 1360 行 runtime parity 后，才能替换 final bundle。安全加固代码已将缺失 policy、score、threshold 和非法风险值改为 fail-closed，并要求部署包/checkpoint 完整且结构严格匹配；这解决的是运行安全合同，不能替代尚未完成的 C5-only runtime parity。

### `gaps_deploy/` 主线文件

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_deploy/final_runtime.py` | 必须保留 | 最终部署 runtime，统一输出 schema，包含 `auto_output_ppm`。 |
| `gaps_deploy/rich_residual.py` | 必须保留 | H2.3/H8 及历史 H8+C4 rich residual/direct-head runtime policy。 |
| `gaps_deploy/r4a_residual.py` | 必须保留 | `inference.py` 直接依赖的 R4a residual artifact 加载与推理逻辑；已纳入 Git 干净检出闭包。 |
| `gaps_deploy/inference.py` | 必须保留 | 基础部署 predictor：分类→回归→校准→QC。 |
| `gaps_deploy/deploy_config.py` | 必须保留 | 部署配置读取和路径管理。 |
| `gaps_deploy/calibration.py` | 必须保留 | bias/affine/full/specialist 等校准逻辑。 |
| `gaps_deploy/qc_policy.py` | 必须保留 | risk score 与 accept/review/reject 决策。 |
| `gaps_deploy/build_package.py` | 保留 | 构建部署包基础工具。 |
| `gaps_deploy/build_per_client_packages.py` | 保留 | 多 client 部署包构建。 |
| `gaps_deploy/validate_deployment_packages.py` | 保留 | 部署包结构预检。 |
| `gaps_deploy/predict_client_file.py` | 保留 | 文件级批量推理入口。 |

### 部署导出、验证与 benchmark

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `export_h8_formal_c4_rescue_deployment_candidate.py` | 历史部署证据 | 导出旧 H8+C4 v2 candidate；不作为当前 C5-only 最终包。 |
| `export_h8_source_aug_deployment_candidate.py` | 保留 | H8 source-aug bundle 导出。 |
| `export_target_ridge_profile_artifact.py` | 保留 | target Ridge profile artifact 导出。 |
| `validate_rich_residual_runtime_candidate.py` | 必须保留 | runtime vs analysis parity 验证。 |
| `compare_h8_runtime_equivalence.py` | 必须保留 | H8 runtime 等价性检查。 |
| `audit_h8_c4_guardrails.py` | 历史审计 | C4 rescue gate false hit / nonCO guardrail audit。 |
| `validate_feature_schema.py` | 必须保留 | feature/runtime schema 验证。 |
| `benchmark_runtime_profiles.py` | 必须保留 | bundle size / CPU latency benchmark。 |
| `scripts/build_final_deployment_package.py` | 保留 | final deployment bundle 构建入口。 |
| `scripts/validate_final_deployment_bundle.py` | 保留 | final deployment bundle 预检入口。 |

部署输出字段当前应使用：

```text
gas_class
gas_name
class_prob
base_r3ak16_raw_ppm
routed_pred_ppm
final_ppm
co_corrected_ppm
auto_output_ppm
qc_decision
risk_score
```

其中 `auto_output_ppm` 只在 `qc_decision == accept` 时作为系统自动输出；`final_ppm` 和 `co_corrected_ppm` 保留用于审计。

## 6. QC、可靠性与报告

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `gaps_deploy/build_qc_policy_from_predictions.py` | 保留 | 从 calibration prediction 构建 QC policy。 |
| `gaps_deploy/run_qc_guardrail_validation.py` | 保留 | QC guardrail 验证。 |
| `gaps_deploy/evaluate_qc_status_metrics.py` | 保留 | accept/review/reject 后指标统计。 |
| `summarize_qc_posthoc_profiles.py` | 保留 | post-hoc QC profile 汇总。 |
| `run_profile_qc_coverage_audit.py` | 保留 | QC 覆盖率和可靠性审计。 |
| `scripts/evaluate_coverage_review_qc.py` | 保留 | coverage/review 口径评估。 |

报告/证据打包相关：

| 文件 | 当前建议 | 作用 |
|---|---|---|
| `run_current_base_evidence_freeze.py` | 保留 | 当前证据冻结包。 |
| `run_current_base_method_story.py` | 保留 | 方法故事线材料生成。 |
| `run_current_base_paper_story_pack.py` | 保留 | 论文/报告资料包。 |
| `run_final_metric_consolidation_20260709.py` | 保留 | 最终指标整合。 |
| `scripts/summarize_iotj_classification_ablation.py` | 当前分类表入口 | 生成 C5-only 分类行流、单次指标和多种子 mean/std。 |
| `docs/paper/iotj_system_methodology_20260711.zh.md` | 当前方法底稿 | 系统原理、公式、参数、实现映射、主张边界和审查风险。 |
| `docs/experiments/iotj_system_experiment_notebook.md` | 必须持续更新 | 记录命令、输入、输出、失败、决定和下一步 gate。 |
| `summarize_final_gaps_evidence.py` | 保留 | GAPS 证据汇总。 |
| `scripts/build_meeting_report_visuals.py` | 保留 | 会议图表生成。 |

## 7. 历史/归档/不建议作为主入口

这些文件并非没用，但不建议作为当前主线入口：

```text
client.py
server.py
exp_improved.py
adaptation.py
preprocessor.py
route_aware_response_anchoring.py
train_flower_local_fedavg.py
train_simplified_classifier_baseline.py
run_time_aware_split_backbone_gain_isolation.py
run_weighted_regression_loss_ablation.py
run_co_specific_correction_eval.py
run_co_specific_client_extension_eval.py
run_formal_c4_route_rescue_selector.py
run_formal_c4_rescue_auto_v2_eval.py
export_h8_formal_c4_rescue_deployment_candidate.py
run_c5_*_eval.py
codex_web_profile_reset.ps1
codex_force_web_profile_reset.ps1
codex_shell_proxy_setup.ps1
Start-Codex-With-Shell-Proxy.cmd
_local_archive_20260626/
历史版本代码备份归档/
```

其中 `client.py` 仍被 Flower task 复用本地损失实现，不能直接删除；早期 `server.py/exp_improved.py` 主要是历史参考。当前云边端入口应优先看 `gaps_flower/` 和 `scripts/run_iotj_classification_cloud_edge.py`。大量旧 `run_c5_*`、`diagnose_*`、`audit_*` 文件属于探索性诊断，适合保留但不应放进论文主流程图。

## 8. 当前有用代码文件最小清单

如果只保留“能复现当前主线”的最小代码集合，建议至少包括：

```text
config.py
model.py
utils.py
federated_dataset.py
preprocessor_time_aware.py
split_dataset.py

gaps_flower/task.py
gaps_flower/client_app.py
gaps_flower/server_app.py
gaps_flower/strategy.py
gaps_flower/domain_adaptation.py
gaps_flower/evaluate_checkpoint.py
gaps_flower/regression_task.py
gaps_flower/calibration_fit.py
gaps_flower/specialist_calibration_fit.py

gaps_deploy/final_runtime.py
gaps_deploy/rich_residual.py
gaps_deploy/inference.py
gaps_deploy/deploy_config.py
gaps_deploy/calibration.py
gaps_deploy/qc_policy.py
gaps_deploy/build_package.py
gaps_deploy/build_per_client_packages.py
gaps_deploy/validate_deployment_packages.py
gaps_deploy/predict_client_file.py

scripts/generate_iotj_classification_ablation_commands.py
scripts/run_iotj_classification_cloud_edge.py
scripts/summarize_iotj_classification_ablation.py
scripts/audit_iotj_experiment_inputs.py
scripts/remote_launch_flower_server_clean.py
scripts/remote_launch_flower_client_clean.py
scripts/audit_target_split.py
scripts/merge_target_splits.py
scripts/build_final_deployment_package.py
scripts/validate_final_deployment_bundle.py
scripts/build_iotj_c5_regression_inputs.py
scripts/run_iotj_c5_h23_plus.py
scripts/select_iotj_c5_p4.py

run_formal_target_ridge_auto_v2_eval.py
run_formal_target_mlp_auto_v2_eval.py
run_h2_3_plus_fusion_profile.py
run_source_augmented_target_ridge_eval.py
validate_rich_residual_runtime_candidate.py
compare_h8_runtime_equivalence.py
validate_feature_schema.py
benchmark_runtime_profiles.py
run_final_metric_consolidation_20260709.py

docs/paper/iotj_system_methodology_20260711.zh.md
docs/superpowers/plans/2026-07-11-iotj-system-experiment-closure.md
docs/experiments/iotj_system_experiment_notebook.md
```

## 9. 文件管理建议

当前 Git 工作区里有大量未跟踪的实验脚本和结果辅助脚本。建议按三类处理：

1. 主线必须保留并提交：`gaps_flower/`、`gaps_deploy/`、`scripts/remote_launch_*`、`scripts/evaluate_clean_matrix_remote.py`、最终汇总/验证脚本。
2. 诊断脚本保留但集中到 `diagnostics/` 或 `experiments/`：`diagnose_*`、`audit_*`、`run_c5_*`、`run_low_calibration_*`。
3. 本地工具或历史材料归档：Codex profile reset 脚本、旧单机 smoke、历史版本代码备份。

不要提交大体积数据和结果：

```text
dataset/
results/大型中间文件
deployment bundle
checkpoint .pth
临时日志
```

如果需要共享结果，优先提交轻量 summary：

```text
results/*summary*.md
results/*metrics*.csv
docs/*.md
```

## 18. 2026-07-13 正式回归闭环入口

当前有效的正式回归证据目录是：

```text
results/iotj_c5_formal_regression_20260713_v2
results/iotj_c5_formal_regression_20260713_v2_summary
```

不带 `_v2` 的 `results/iotj_c5_formal_regression_20260713` 仅保留为失败审查记录：其第一版统一流缺失 R1 rich-only Ridge 列，不能用于论文指标。

| 文件 | 当前作用 |
|---|---|
| `scripts/run_iotj_c5_regression_cloud.py` | 阿里云 ECS 正式回归同步、顺序执行、完整性检查和结果回收。 |
| `scripts/run_iotj_c5_regression_suite.py` | 串联 C5 输入、H2.3+、无 C4 rescue 的 H8、全 1360 条 test oracle-route 诊断流、QC 和 R0-R7。 |
| `scripts/assemble_iotj_c5_regression_ladder.py` | 生成 R0-R7，R6 只用 calibration-validation 选阈值，R7 明确为离线 oracle。 |
| `scripts/evaluate_iotj_high_coverage_qc.py` | 生成 FULL、HC95、HC90、固定覆盖率排序诊断和 1000 次随机拒绝对照；同一决策子集同时报告 Accepted、Nonreject（Accept+Review）的实际路由与 oracle-route RMSE/NRMSE。 |
| `scripts/summarize_iotj_c5_formal_regression.py` | 校验并汇总 A6/B5/B2 的 168 行回归指标和 9 行扩展 QC 指标，生成 `qc_operational_comparison.csv` 与 `formal_regression_report.md`，并记录源文件哈希。 |

正式 seed-42 预声明观察：B5 固定 H8 的 S_ALL RMSE 为 `17.4473`，S_CC N=`1345`、RMSE=`11.3890`；B5-HC95 实际自动覆盖率为 `96.25%`，accepted RMSE 为 `15.9075`。探索性 B2 重放的固定 H8 S_ALL RMSE 为 `14.6564`，S_CC N=`1350`、RMSE=`11.3288`；B2-HC95 自动覆盖率为 `95.66%`，accepted RMSE 为 `12.6729`，并标记 `7/10` 个错路由窗口。B2 的更好 S_ALL 主要来自更少的分类错误，仍需种子 43-46 配对确认，不能直接表述为稳定显著提升。

2026-07-14 扩展口径：FULL 下把全部 `1360` 条 test 窗口强制设为 `route_class=true_class` 后，A6/B5/B2 共用 H8 的 RMSE/NRMSE 均为 `11.9082/0.0690`。该 oracle-route 指标保留所有窗口，只替换路由，因此不同于仅统计分类正确窗口的 `S_CC`。HC95/HC90 同时报告 automatic yield（Accept/N）和 nonreject coverage（(Accept+Review)/N），可分别回答“可自动输出多少”和“仅需人工复核、无需直接拒绝后可保留多少”。原 H8 预测、风险策略、工作点样本数和实际路由指标均未改变。

## 19. 2026-07-13 系统安全加固状态

安全加固从提交 `6b6d930` 到 `dfb43f1` 依次关闭 QC fail-open、随机模型/宽松 checkpoint 加载、phase 双路径差异、DA 延迟输入失败、specialist 真值路由/切分泄漏、回归 FedAvg 样本权重漂移，以及审查发现的 ranking 零值和 non-strict DA 旁路；部署依赖闭包在当前修订中补齐。冻结的 B1-B5 manifest、legacy DA loss 默认语义、正式 R0-R7 定义和既有结果文件均未修改。

当前验证证据：

- 部署安全与干净 Git 检出：`88 passed`；其中索引树 import/CLI 冒烟为 `4 passed`。
- Flower 分类/DA：`62 passed, 2 warnings`。
- specialist/回归运行时：`44 passed`。
- 冻结实验合同：`111 passed, 2 warnings`。
- 当前全部已跟踪测试：`322 passed, 2 warnings`。
- 改动 Python 文件：`compileall` 通过 28 个文件。

剩余边界：这些修复允许主张“代码路径已 fail-closed、资产可验证、干净检出可运行”，但在 C5-only 最终策略冻结、1360 行 runtime numerical parity、Pi/PC 性能与可用性测试完成前，仍不能主张最终部署系统已经完成。

## 20. 2026-07-17 Spec A Confirmation Observability Framework

本节描述的是确认实验的可观测、冻结、验证与汇总框架，不是正式确认结果。Task 1--9 的批准代码头为 `12b3bc45dd8ceff7098e543cf94d789a2eb338d7`；后续 candidate 曾推进到 `7ec77e3`，但 B5 formal-topology OFF/ON 在 round 2 出现数值分叉，因此该 revision 不能成为 final confirmation freeze。正式 B2/B5 十运行和 sealed C5 target-test evaluation 均尚未开始。`config.py`、`client.py`、`model.py`、`federated_dataset.py`、`gaps_flower/task.py`、`gaps_flower/domain_adaptation.py` 相对批准基线 `a920ecdbdbea250220343d63926cb370178cdc5e` 没有变化。

### 20.1 新增/改动文件的职责与公共用法

| 文件 | 职责 | 输入 | 输出/副作用 | 公共用法 |
|---|---|---|---|---|
| `requirements-confirmation.txt` | 冻结 Spec A 附加依赖版本 | `requirements.txt` 环境 | `flwr==1.23.0`、`protobuf==4.25.8`、`psutil==7.0.0` | `python -m pip install -r requirements-confirmation.txt` |
| `gaps_flower/observability.py` | append-only canonical JSONL、process-local sequence、延迟 overhead 自计量和 close summary | `ObserverIdentity` context JSON、独占 events path | `events.jsonl` 与同 stem 的 `events.close.json` | `load_observer(context_path, events_path)`；禁用时两参数都省略，启用时必须成对提供 |
| `gaps_flower/flower_message_audit.py` | 对真实 legacy Flower `FitIns/FitRes` 做不变性审计 | `FitIns` 或 `FitRes` | `MessageAudit(logical, application_message_bytes, application_message_sha256, flower_serialize_ns)` | `audit_fit_ins`、`audit_fit_res`、`canonical_fit_ins_bytes`、`canonical_fit_res_bytes` |
| `gaps_flower/client_app.py` | client observer 接入 | server round/config、client identity、observer CLI context/events | `client_fit_start/train_start/train_end/fit_end` 事件；训练返回值不变 | 正常 `python -m gaps_flower.client_app ...`；只由 controller 附加 `--observer-context/--observer-events` |
| `gaps_flower/strategy.py` | server round、FitIns/FitRes、aggregate/DA phase 接入 | 当前 Flower strategy 输入和可选 observer | server phase/message 事件；observer 关闭时算法行为保持原样 | 由 `server_app` 构造 strategy；不要直接手写 sidecar |
| `gaps_flower/server_app.py` | server observer CLI、构造、异常记录与关闭 | 原 server 参数，加成对 observer 参数 | ECS `events.jsonl`/close；producer failure 也保留 | 正常 server CLI；observer 参数由 controller 生成 |
| `scripts/sample_iotj_process_resources.py` | 独立 1 Hz 进程树 sampler，排除自身，读取稳定 PID/create-time、RSS/CPU/thread 与 Pi thermal/throttling | target `--pid`、context/events、interval、stop file | `resource.jsonl`、`resource.close.json`、durable `resource_sampler_end` | `python -m scripts.sample_iotj_process_resources --pid ... --observer-context ... --observer-events ...` |
| `scripts/freeze_iotj_confirmation_protocol.py` | 事务式冻结 exact Git source、active dataset、算法 config 与十运行 schedule | 40-hex confirmation commit、C1/C2/C5 data root、三个新输出根 | source tar、source/dataset/protocol manifests、十个 command manifests；拒绝覆盖 | `python -m scripts.freeze_iotj_confirmation_protocol ...` |
| `scripts/run_iotj_confirmation_observability.py` | fail-closed controller、content-addressed 三主机部署、preflight、attempt 分配、进程 ownership、evidence recovery、validator seam | 五个互相绑定的 frozen inputs、run allowlist、ECS/Pi/PC runtime | immutable attempt registry/status chain、raw sidecars、checkpoint、`attempt_audit.json` | `--validate-inputs-only` 是纯本地 queue Gate；`--preflight-only` 联系三主机；无 mode flag 才执行正式 queue |
| `scripts/validate_iotj_confirmation_attempt.py` | 对一次 attempt 做 schema/identity/message/phase/resource/overhead 的结构验证 | attempt dir、protocol manifest、新 audit path | canonical `iotj.confirmation.attempt_audit.v1`；valid exit 0，invalid exit 2；拒绝覆盖 | `python -m scripts.validate_iotj_confirmation_attempt --attempt-dir ... --protocol-manifest ... --output ...` |
| `scripts/summarize_iotj_confirmation_observability.py` | sealed canonical-only C5 evaluator与五种子系统汇总 | raw root、protocol、data root、新 output root、device/batch | 分类、通信、时延、资源、overhead、registry、input manifest 与 claim map | 仅在 exact B2/B5 x seeds 42--46 十个 canonical Gate 后运行 |
| `scripts/diagnose_iotj_a003_timing.py` | 只读诊断未完成/failed confirmation attempt 的逐轮慢点 | attempt `raw/ecs`、`raw/pi`（或 `client_c1`）、`raw/pc` JSONL 与两轮 pilot CSV | 逐轮 wall/train/fit/DA/aggregate/waiting residual/resource CSV 与 pilot 对比 Markdown | `python -m scripts.diagnose_iotj_a003_timing --attempt-dir <failed-attempt> --pilot-round-csv <pilot.csv> --output-root <new-dir>`；不读取 test/checkpoint、不启动训练 |
| `scripts/run_iotj_observer_equivalence_gate.py` | OFF-A/ON/OFF-B 数值等价 Gate；另提供 noncanonical formal smoke hook | B2/B5、新 output root；formal mode 再加 protocol manifest | 本地 `observer_equivalence_report.json`（formal 为 `formal_observer_equivalence_report.json`）、独立 common trace、三 attempt evidence | 无 `--formal-topology` 为安全本地两 client/两 round synthetic Gate；有该 flag 会联系 ECS/Pi/PC |

对应九个测试模块分别覆盖 observer、message audit、Flower integration、resource sampler、protocol freezer、controller、attempt validator、sealed summary 和 equivalence Gate。它们是合同测试，不是实验结果。

### 20.2 identity 与 exact sidecar schema

Observer context 只能包含 `run_id, attempt_id, group_id, training_seed, client_id, host_id, producer, confirmation_commit, source_archive_sha256, dataset_manifest_sha256, algorithm_config_sha256`。`run_id` 必须匹配 `c12_to_c5__(b2|b5)__s(42|43|44|45|46)`；`attempt_id` 必须是对应 run 加 `__aNNN`；group/seed 必须由 run identity 唯一推出。commit 为 40 hex，三个内容哈希均为 64 hex。

每条 `iotj.confirmation.observability.v1` JSONL event 的顶层 key 集合必须**恰好**为：

```text
schema_version, event_id, event_type, run_id, attempt_id, group_id,
training_seed, round, client_id, host_id, producer, process_instance_id,
sequence, wall_time_utc, monotonic_ns, confirmation_commit,
source_archive_sha256, dataset_manifest_sha256, algorithm_config_sha256,
status, payload
```

`event_id` 为 `<attempt>/<host>/<producer>/<process_instance>/<sequence>`；每个文件内相同 producer/process 的 sequence 必须从 1 连续递增。`round` 只能是 1--25 或 null；client 只按 producer contract 使用 null/C1/C2（server FitIns 暂以同轮 proxy id 记录并由 FitRes 解析到 C1/C2）；status 只能是 `started/succeeded/failed/aborted`。五个固定 evidence 流及其同 stem close sidecar 为：

```text
raw/ecs/events.jsonl       -> host=ecs, producer=server, client=null
raw/pi/events.jsonl        -> host=pi-c1, producer=client, client=C1
raw/pi/resource.jsonl      -> host=pi-c1, producer=resource_sampler, client=C1
raw/pc/events.jsonl        -> host=pc-c2, producer=client, client=C2
raw/pc/resource.jsonl      -> host=pc-c2, producer=resource_sampler, client=C2
```

每个非 overhead event 必须有且只有一个延迟写入的 `observer_overhead`，其 payload key 恰好是：

```text
observed_event_id, observer_flower_serialize_ns, observer_event_encode_ns,
observer_io_write_ns, observer_fsync_ns, observer_total_ns,
observer_event_bytes_written, observer_event_count
```

close JSON key 恰好是：

```text
schema_version, run_id, attempt_id, host_id, producer, process_instance_id,
observer_flower_serialize_ns, observer_event_encode_ns, observer_io_write_ns,
observer_fsync_ns, observer_total_ns, observer_event_bytes_written,
observer_event_count, observer_reporting_tail_bytes
```

close summary 是总计权威来源，并显式计入最后一个 overhead 的 reporting tail。observer overhead 只单独报告，不能从原始 phase time 中相减来制造“校正后”训练时间。

### 20.3 消息三层口径

FitIns/下行 `downlink_audit` 的 key 恰好为 `logical, application_message_bytes, application_message_sha256`；logical key 恰好为：

```text
logical_downlink_model_value_bytes,
logical_downlink_parameter_blob_bytes,
logical_downlink_semantic_proto_utf8_bytes,
logical_downlink_other_config_value_bytes,
logical_downlink_total_bytes
```

FitRes/上行 `uplink_audit` 具有相同三个顶层 key；logical key 恰好为：

```text
logical_uplink_model_value_bytes,
logical_uplink_parameter_blob_bytes,
logical_uplink_prototype_utf8_bytes,
logical_uplink_prototype_var_utf8_bytes,
logical_uplink_statistics_utf8_bytes,
logical_uplink_diagnostic_value_bytes,
logical_uplink_total_bytes
```

Layer 1 logical total 使用 Flower parameter blob 加对应 UTF-8/scalar 分量；`model_value_bytes` 是反序列化 ndarray value 口径，单列诊断且不重复加到 logical total。Layer 2 是 `ServerMessage(fit_ins=...)` 或 `ClientMessage(fit_res=...)` 的 deterministic protobuf application bytes 与 SHA-256。Layer 3 没有采集，协议、command manifest、attempt registry 和通信表必须保持 `transport_status=not_collected`，不可写成 0 或“无通信”。

### 20.4 resource、attempt 与 canonical 边界

`resource_sample.payload` 必须包含精确数值/身份语义：`root_pid, sampler_pid_excluded, pids, process_identities[{pid,create_time,identity_available}], rss_tree_bytes, rss_tree_peak_bytes, process_count_tree, thread_count_tree, cpu_time_tree_seconds, cpu_time_tree_delta_seconds, cpu_percent_tree_one_core_scale, cpu_percent_tree_host_scale, logical_cpu_count, sample_interval_start_monotonic_ns, sample_interval_end_monotonic_ns, sample_interval_wall_ns, sample_errors`，以及 `cpu_temperature_c/available/source, vcgencmd_available, throttled_raw/bits/available, thermal_errors`。不可用温度、throttling 或 sampler peak RSS 必须用 null + availability/error 说明，不能伪造 0。

`resource_sampler_end.payload` 必须报告 `root_pid, sampler_pid, shutdown_reason, shutdown_error, sample_count, sampler_cpu_user_seconds, sampler_cpu_system_seconds, sampler_rss_peak_bytes/available/method/error, observer_cost_values_scope, observer_event_encode_ns, observer_io_write_ns, observer_fsync_ns, observer_event_bytes_written, observer_event_count, observer_close_summary_path, observer_close_summary_is_authoritative`。Pi 在 Linux 生成、PC 在 Windows 生成的 sidecar 恢复后都必须绑定到 attempt 内上述精确 regular-file 路径。

validator 对 C1、C2 分别只用各自主机 monotonic clock，把 resource interval 与 25 个 `client_fit_start..client_fit_end` 区间连接；每轮至少一个 overlap，按 1 秒 bin 去重后的覆盖率必须 `>=0.95`。sampler PID 不得进入目标进程树；PID 必须结合 create time 防重用；sample error、非有限/错误 numeric type、identity/path/schema 漂移均使 attempt invalid。

每个正式 run 的 attempt 从 `__a001` 递增分配，immutable status lifecycle 只能从 running/start，经 preflight，到 canonical/invalid，或在异常时到 failed/aborted。canonical 必须是 validator `status=valid` 且 audit SHA 绑定；`attempt_audit.json` 顶层恰好记录 `attempt_id, counts, inputs, protocol_manifest_sha256, reasons, resource, run_id, schema_version, status`，其中正式 message matrix 必须是 25 rounds、50 FitIns、50 FitRes。失败 attempt 原地保留 status chain、raw、log 和可用 audit；不能删除、覆盖、用指标挑选或把旧失败改写成 canonical。

sealed summarizer 只接受 B2/B5 x seeds 42--46 各一个 canonical attempt，并二次核对 audit-approved sidecar、checkpoint 与实际消费字节 SHA。通过前不打开 `client_5/test_*`；通过后原子发布 `classification_per_run.csv`、`classification_multiseed_summary.csv`、`flower_communication_per_round.csv`、`flower_communication_summary.csv`、`flower_round_time_breakdown.csv`、`training_resource_summary.csv`、`observer_overhead_summary.csv`、`attempt_registry.csv`、`summary_input_manifest.json`、protocol copy 与 claim 文档。任何读取、hash、evaluation、fsync 或 rename 失败都回滚本次拥有的 stream/summary，而保留原始 attempt evidence。

### 20.5 当前 Gate 结果与不能越过的边界

Task 9 `final_v10` 本地 B2/B5 Gate 均为 `equivalent`、`max_abs_delta=0`、8-message cross matched；B2/B5 report SHA-256 分别为 `1191d766e932360c8ed2e83b9258c3e18c284010ba5dbe5df249e6de8ea48646`、`a7d8a437e87d6703b8255d9431a0d47472a278bbe1d603b5658cbe9eda5d7d96`。这些目录都是 synthetic/local-only/unstaged。

真实三机 formal-topology 的当前结论不同：B2 OFF/ON 已达到 `status=equivalent`、`max_abs_delta=0`；B5 为 `observer_path_mutation`，`max_abs_delta=0.01269597525242716`，所以 fail closed。当前第一处分叉证据显示 round 2 FitRes 到达顺序为 OFF `[C2,C1]`、ON `[C1,C2]`；逐客户端 normalized FitRes 与 post-aggregate/pre-DA checkpoint 相同，最早捕获的数值分叉位于 server DA。fixed-state replay 尚未证明到达顺序会造成最终梯度/checkpoint 分叉，因此不能直接提交排序修复，也不能生成 freeze record 或启动正式十运行。

审查中发现并修复的关键模式包括 RNG 副作用、observer audit 后的 FitRes mutation、live trace 与 sidecar 未交叉绑定、schema/type/identity/regular-file/path 不精确、Pi Linux resource 到 Windows attempt 的恢复路径漂移，以及 DA diagnostic 只比路径而不比引用 JSON bytes/语义。正因为这些失败能揭示真实证据风险，失败 attempt 必须保留而不是清理。

2026-07-17 用户预授权：Tasks 10--12 和每个 fail-closed candidate-freeze、host preflight、B2/B5 formal smoke、hash/schema/runtime identity、dry-run queue Gate 全部通过后，可不再请求手工批准而启动冻结的十个 25-round runs；任一 Gate 失败仍停止并保留证据。历史 `feaa75b`、跨方向 seed-42 和所有本地 Gate 不能替代正式确认。

### 20.6 正式运行会自动记录的系统指标

正式入口是 `scripts/run_iotj_confirmation_observability.py`。Controller 会为 ECS server、Pi C1、PC C2 自动注入 Observer，并为 Pi/PC 自动启动独立 1 Hz 进程树 sampler；不应在训练结束后重新跑同一训练补采指标。

| 输出 | 主要内容 | 采集/汇总代码 |
|---|---|---|
| `flower_communication_per_round.csv` | 每轮、每客户端的 logical FitIns/FitRes 分量，serialized application-message uplink/downlink bytes 与 SHA-256 | `gaps_flower/flower_message_audit.py`、`gaps_flower/strategy.py`、sealed summarizer |
| `flower_communication_summary.csv` | 每个 25-round run 的 logical/application uplink、downlink、total 与 bytes/round | sealed summarizer；transport 固定为 `not_collected` |
| `flower_round_time_breakdown.csv` | C1/C2 train core、fit callback、并行 critical path、server aggregate、server DA、non-DA、round wall；逐轮及 mean/p50/p95/total | `client_app.py`、`strategy.py`、sealed summarizer |
| `training_resource_summary.csv` | Pi/PC 全采样及训练重叠 RSS、peak RSS、单核/整机 CPU mean/peak、采样覆盖率、Pi temperature/throttling | `scripts/sample_iotj_process_resources.py`、validator、sealed summarizer |
| `observer_overhead_summary.csv` | Flower serialization、event encoding、write、fsync、事件/字节数及 overhead/round-wall ratio | `gaps_flower/observability.py`、resource sampler、sealed summarizer |
| `attempt_registry.csv` | canonical/failed/invalid 状态及 commit、archive、dataset、config、protocol、audit、checkpoint、prediction-stream SHA-256 | controller、validator、sealed summarizer |
| `classification_per_run.csv` / `classification_multiseed_summary.csv` | Accuracy、Macro-F1、NLL、ECE、per-class recall、五 seed mean/sample std 与 B2-B5 paired delta | 仅在十个 canonical attempts 齐全后由 sealed summarizer 打开 C5 test 生成 |

资源原始流还包含进程树 PID/create-time、process/thread count、CPU time 与 sampling errors。正式有效性要求 C1/C2 每轮训练区间都有采样重叠，1 秒 bin 覆盖率至少 `0.95`；Observer 自身开销单独报告，不从训练时间中相减。

### 20.7 推荐使用顺序与分支位置

当前开发/执行位置是独立 worktree：

```text
.worktrees/iotj-confirmation-observability
branch: codex/iotj-confirmation-observability
```

仓库根目录若仍检出 `codex/system-safety-hardening`，其中同名 `代码文件介绍.md` 不包含本节，不能据此判断 Spec A/P0 的最新状态。正式公共入口只使用 controller，不手写 Observer sidecar：

```text
freeze protocol/archive
-> controller --validate-inputs-only
-> controller --preflight-only
-> B2/B5 --formal-topology smoke
-> 两个 smoke 均 exact equivalent 后，无 mode flag 启动正式 queue
-> exact ten canonical attempts 后运行 sealed summarizer
```

当前固定执行顺序为：

```text
P0 B5 blocker 最小修复
-> B2/B5 formal smoke
-> final confirmation revision/archive freeze
-> B2/B5 x seeds 42--46 十个 25-round canonical runs
-> sealed C5 evaluation 与 classifier/prediction streams 冻结
-> A/B/C/D source-head dependency paired ablation
-> 冻结 final regression architecture
-> 必要时 distributed sufficient-statistics Ridge
-> 12/24/48/80/Full low-calibration
-> final C5 bundle -> 1360-row parity -> Pi/PC inference benchmark
```

P0 完成前不得运行 A--E、正式 low-calibration 或 final bundle；A/B/C/D 必须固定 classifier checkpoint、logits/probabilities、predicted route、sample keys、calibration split、regression seeds 和 test seal，真正删除对应 source feature 后重新拟合 target head，禁止以填 0 模拟删除。

### 20.8 B5 到达顺序非确定性修复与诊断入口

真实 OFF-A/OFF-B 已证明 Observer 不是根因。两个 OFF 的 initial checkpoint、round-1 FitIns、C1/C2 FitRes 与 plain aggregate 完全一致，但 Flower 完成顺序分别为 `[C2,C1]` 和 `[C1,C2]`；未经规范化的顺序首先改变 semantic-prototype float32 reduction，再传播到 DA。

生产修复位于 `gaps_flower/strategy.py`：

- `canonicalize_fit_results(results)`：从每个 FitRes 的 `metrics.client_id` 读取稳定身份；缺失、非正整数或重复时 fail closed；否则按 client ID 升序返回。
- `GapsStrategy.aggregate_fit()`：在参数聚合、metrics、prototype/statistics、semantic update 和 server DA 之前只调用一次上述规范化。禁止在下游各自排序，以免各阶段合同不一致。

诊断/复现工具：

- `scripts/diagnose_b5_fixed_state_order_replay.py`：对保留的 B5 证据执行 prototype-only 与完整 one-step DA 固定状态重放，验证 batch/RNG/参数边界；只写新的 sibling output，不覆盖 reference。
- `scripts/run_iotj_formal_off_repeat_probe.py`：使用 `a900..a997` 的独立非 canonical 身份运行单个 formal OFF repeat；`a998/a999` 继续保留给标准 OFF/ON smoke。示例：

```text
python -m scripts.run_iotj_formal_off_repeat_probe \
  --attempt-number 997 \
  --output-root results/iotj_main_confirmation_observability_20260715/smoke/<new-probe-root>
```

结构化根因报告：`results/iotj_main_confirmation_observability_20260715/smoke/b5_formal_off_repeat_a997_7ec77e3_v2/b5_first_divergence_report.json`。

### 20.9 冻结 confirmation 版本与运行入口

最终算法源码不是当前文档 HEAD，而是冻结 commit `2ef7aea77b9dfabdd09da4f38742907a37c58c30`。唯一 archive 与控制文件为：

- `results/c2e/source/confirmation_source.tar`：三机实际部署源码，SHA-256 `52bdbf96568014cc363f0ce3c666026be29f5f0279c7a130b41458d42a0d0c68`；
- `results/c2e_summary/`：protocol、source、dataset manifests 与 `confirmation_freeze_record.json`；
- `results/c2e_commands/`：B2/B5 × seeds 42--46 的十个不可变命令 manifest；
- `results/c2e_smoke_b2/`、`results/c2e_smoke_b5/`：真实 ECS + Pi + PC OFF/ON Gate 证据，二者均 `max_abs_delta=0`。

2026-07-19 状态修订：真实三机 B2-s42 的 a001/a002 为网络失败，a003 在 22 轮后触发旧 5 h Controller deadline，a004 虽完成 25 轮但 C2 resource coverage `0.938536 < 0.95` 而 invalid；因此目前 canonical confirmation 是 `0/10`，不得运行 sealed summarizer。a003 的只读慢点产物位于 `results/iotj_a003_timing_diagnosis_20260719/`；推荐的未执行双轨方案在同目录的 `track_a_track_b_execution_design.md`：Track A 仅用于快速多种子算法统计，Track B 仅用于 B2/B5 各一个真实 ECS+Pi+PC 代表性系统表，二者必须分别冻结 execution-topology manifests。

冻结输入检查命令：

```text
python -m scripts.run_iotj_confirmation_observability \
  --protocol-manifest results/c2e_summary/confirmation_protocol_manifest.json \
  --source-archive-manifest results/c2e_summary/source_archive_manifest.json \
  --dataset-manifest results/c2e_summary/dataset_manifest.json \
  --command-root results/c2e_commands \
  --source-archive results/c2e/source/confirmation_source.tar \
  --validate-inputs-only
```

去掉 `--validate-inputs-only` 才会启动 canonical queue。运行期间 Observer 会同步记录 Flower serialized application uplink/downlink bytes、round/local train/server aggregation/server DA/wall time、Pi/PC RSS/CPU、Pi temperature 和 Observer 自身开销。不要把两轮 smoke 指标混入正式 25-round summary，也不要改用后续文档 commit 重新打包源码。
## 21. ECS-C2 + Pi 代表性实验入口（2026-07-20）

当前真实运行入口仍是 `scripts/run_iotj_confirmation_observability.py`。它不实现新的模型，而是把冻结训练命令安全地放到三台主机运行，并完成 preflight、只追加 attempt 状态、Observer 注入、1 Hz 资源采样、远端进程所有权、证据回收和 validator 调用。

这次增加/固化的执行层功能：

- `scripts/run_iotj_classification_cloud_edge.py`：树莓派热点 SSH 握手允许 20 秒连接预算，单次 30 秒超时不会绕过总等待窗口，而是继续重试；
- `scripts/run_iotj_confirmation_observability.py`：远端 ECS-C2/Pi/server attempt 目录使用 `topology_id + attempt_id + controller_owner_instance_id`，避免新的本地 a005 与历史其他拓扑同名目录相撞；本地保存 `remote_attempt_binding.json`；
- `scripts/validate_iotj_confirmation_attempt.py`：没有 topology binding 时只接受历史 `raw/pc`/`pc-c2`，有经过 hash 校验的 ECS-C2 binding 时才接受 `raw/ecs_c2`/`ecs-c2`，禁止根据目录名静默猜测拓扑；
- `results/iotj_ecs_c2_representative_20260720/monitor_b2_s42_a005.ps1`：读取本地 binding 后同时查看正式状态、server 已完成轮数、Pi/ECS-C2 客户端尾日志和资源流大小。

训练设置没有变化：B2/B5 各 25 rounds、5 local epochs、batch 32，C1/C2 数据、模型、loss、optimizer 和 server DA 均来自 SHA-256 固定的 archive/manifests。当前只改变 C2 的执行主机，因此论文中应称为 `ECS server + Raspberry Pi C1 + ECS C2 representative execution topology`，不能称为原 Pi+PC 拓扑。

完整复制命令、启动顺序、成功判据、监控方法和断网处理见 `docs/experiments/iotj_ecs_c2_runbook_20260720.zh.md`。当前 B2-s42/a005 已启动；B5 不自动串行，必须等 B2 回收并通过 validator 后再单独启动。
