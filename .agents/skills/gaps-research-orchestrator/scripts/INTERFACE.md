# Future script interface

Reserved for read-only artifact presence and schema-status checks from explicitly supplied paths. It may draft status to a new destination; it must not call child logic, infer experiment semantics, or edit project artifacts.
