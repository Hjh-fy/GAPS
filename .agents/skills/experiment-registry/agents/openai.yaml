interface:
  display_name: "Experiment Registry"
  short_description: "Register traceable GAPS experiment metadata safely"
  default_prompt: "Use $experiment-registry to draft a traceable record for a confirmed GAPS experiment."
