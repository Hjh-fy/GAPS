# Future script interface

Reserved for read-only candidate collection from explicitly supplied config, manifest, git, and result paths. It may emit a candidate CSV to a new destination; it must not infer semantics, write the canonical registry, or overwrite files.
