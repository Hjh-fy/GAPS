# Future script interface

Reserved for one explicitly selected, stable CSV/JSON schema. Inputs must include confirmed experiment and metric metadata. Outputs go to a new user-selected path; no discovery scan or source-file modification is allowed.
