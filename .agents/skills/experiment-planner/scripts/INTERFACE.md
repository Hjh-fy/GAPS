# Future script interface

Reserved for a later validator that accepts explicit plan and matrix paths, performs schema-only checks, writes to a user-selected new path, and never scans or modifies `results/`.
