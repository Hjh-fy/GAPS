# Future script interface

Reserved for deterministic checks against an explicitly supplied registry subset. It may report missing fields and mismatches to a new file; it must not select experiments, infer semantics, recompute metrics, or alter assets.
