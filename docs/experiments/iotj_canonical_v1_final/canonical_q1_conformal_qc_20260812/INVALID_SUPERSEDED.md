# Q1 v1 invalid and superseded

Status: `INVALID_SUPERSEDED_BY_Q1_V2`.

The first formal execution stopped after writing its preflight receipt, policy
lock, and target-test release receipt. Evaluation then failed because
`empirical_interval` converted a valid per-sample radius vector to `float`.
No Q1 decision or completed evidence bundle was produced. The partial result
directory is preserved and must not be resumed or interpreted scientifically.

The registered replacement is the independently versioned Q1-v2 study at
`canonical_q1_conformal_qc_v2_20260812`. The method, nominal coverage,
threshold, fixed equal weights, inputs, and target split are unchanged.
