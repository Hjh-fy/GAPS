# Canonical v1 Ablation Plan

No new ablation or preprocessing search is authorized. The only diagnostic comparison is the already frozen provenance table for Legacy, continuous interpolation, and canonical 5-Hz preprocessing. It cannot support an algorithm-improvement claim because preprocessing and split provenance differ. HZ2_MEAN_W10S remains an engineering fallback record and receives no formal training.
