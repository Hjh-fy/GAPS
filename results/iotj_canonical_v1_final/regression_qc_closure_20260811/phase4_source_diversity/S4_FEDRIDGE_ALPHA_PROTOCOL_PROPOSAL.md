# S4 FedRidge alpha protocol proposal

`STOPPED_BEFORE_FIT`: the frozen S2 H1 source uses historical 100x8 windows,
while the S4 role view is canonical 50x8. The proposal is identical to the
audited document under `docs/experiments/.../regression_qc_closure/` and requires
manual approval before reconstructing both S2 and S4. C5 remains excluded from
source fit and alpha selection; no target-test selection is allowed.
